"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: D410, E501, E101, D206
from apimatic_core.authentication.multiple.or_auth_group import (
    Or,
)
from apimatic_core.authentication.multiple.single_auth import (
    Single,
)
from apimatic_core.request_builder import RequestBuilder
from apimatic_core.response_handler import ResponseHandler
from apimatic_core.types.parameter import Parameter

from adyen.api_helper import APIHelper
from adyen.apis.base_api import BaseApi
from adyen.configuration import Server
from adyen.http.http_method_enum import HttpMethodEnum
from adyen.models.checkout_forward_response import (
    CheckoutForwardResponse,
)
from adyen.models.list_stored_payment_methods_response import (
    ListStoredPaymentMethodsResponse,
)
from adyen.models.stored_payment_method_resource import (
    StoredPaymentMethodResource,
)


class RecurringApi(BaseApi):
    """A Controller to access Endpoints in the adyen API."""

    def __init__(self, config):
        """Initialize RecurringApi object."""
        super(RecurringApi, self).__init__(config)

    def post_forward(self,
                     idempotency_key=None,
                     body=None):
        """Perform a POST request to /forward.

        Forwards the payment details you stored with Adyen to a third-party that you
        specify and returns the response from the third-party. Supports forwarding
        stored card details or [network
        tokens](https://docs.adyen.com/online-payments/network-tokenization). For
        more information, see [Forward stored payment
        details](https://docs.adyen.com/online-payments/tokenization/forward-payment-d
        etails).

        Args:
            idempotency_key (str, optional): A unique identifier for the message with
                a maximum of 64 characters (we recommend a UUID).
            body (CheckoutForwardRequest, optional): The request body parameter.

        Returns:
            CheckoutForwardResponse: Response from the API. OK - the request has
                succeeded.

        Raises:
            APIException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/forward")
            .http_method(HttpMethodEnum.POST)
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .header_param(Parameter()
                .key("Idempotency-Key")
                .value(idempotency_key))
            .body_param(Parameter()
                .value(body))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .body_serializer(APIHelper.json_serialize)
            .auth(Or(Single("BasicAuth"), Single("ApiKeyAuth"))),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(CheckoutForwardResponse.from_dictionary),
        ).execute()

    def get_stored_payment_methods(self,
                                   shopper_reference=None,
                                   merchant_account=None):
        """Perform a GET request to /storedPaymentMethods.

        Lists the tokens for stored payment details for the shopper identified in the
        path, if there are any available. The token ID can be used with payment
        requests for the shopper's payment. A summary of the stored details is
        included.

        Args:
            shopper_reference (str, optional): Your reference to uniquely identify
                this shopper, for example user ID or account ID. Minimum length: 3
                characters. > Your reference must not include personally identifiable
                information (PII), for example name or email address.
            merchant_account (str, optional): Your merchant account.

        Returns:
            ListStoredPaymentMethodsResponse: Response from the API. OK - the request
                has succeeded.

        Raises:
            APIException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/storedPaymentMethods")
            .http_method(HttpMethodEnum.GET)
            .query_param(Parameter()
                .key("shopperReference")
                .value(shopper_reference))
            .query_param(Parameter()
                .key("merchantAccount")
                .value(merchant_account))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Or(Single("BasicAuth"), Single("ApiKeyAuth"))),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(ListStoredPaymentMethodsResponse.from_dictionary),
        ).execute()

    def post_stored_payment_methods(self,
                                    idempotency_key=None,
                                    body=None):
        """Perform a POST request to /storedPaymentMethods.

        Creates a token to store the shopper's payment details. This token can be
        used for the shopper's future payments.

        Args:
            idempotency_key (str, optional): A unique identifier for the message with
                a maximum of 64 characters (we recommend a UUID).
            body (StoredPaymentMethodRequest, optional): The request body parameter.

        Returns:
            StoredPaymentMethodResource: Response from the API. Created - the request
                has been fulfilled and has resulted in one or more new resources
                being created.

        Raises:
            APIException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/storedPaymentMethods")
            .http_method(HttpMethodEnum.POST)
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .header_param(Parameter()
                .key("Idempotency-Key")
                .value(idempotency_key))
            .body_param(Parameter()
                .value(body))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .body_serializer(APIHelper.json_serialize)
            .auth(Or(Single("BasicAuth"), Single("ApiKeyAuth"))),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(StoredPaymentMethodResource.from_dictionary),
        ).execute()

    def delete_stored_payment_methods_stored_payment_method_id(self,
                                                               stored_payment_method_id,
                                                               shopper_reference,
                                                               merchant_account):
        """Perform a DELETE request to
        /storedPaymentMethods/{storedPaymentMethodId}.

        Deletes the token identified in the path. The token can no longer be used
        with payment requests.

        Args:
            stored_payment_method_id (str): The unique identifier of the token.
            shopper_reference (str): Your reference to uniquely identify this
                shopper, for example user ID or account ID. Minimum length: 3
                characters. > Your reference must not include personally identifiable
                information (PII), for example name or email address.
            merchant_account (str): Your merchant account.

        Returns:
            void: Response from the API. No Content - look at the actual response
                code for the status of the request.

        Raises:
            APIException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/storedPaymentMethods/{storedPaymentMethodId}")
            .http_method(HttpMethodEnum.DELETE)
            .template_param(Parameter()
                .key("storedPaymentMethodId")
                .value(stored_payment_method_id)
                .should_encode(True))
            .query_param(Parameter()
                .key("shopperReference")
                .value(shopper_reference))
            .query_param(Parameter()
                .key("merchantAccount")
                .value(merchant_account))
            .auth(Or(Single("BasicAuth"), Single("ApiKeyAuth"))),
        ).execute()
