"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.links_element_6 import LinksElement6
from adyen.models.links_element_15 import LinksElement15
from adyen.models.links_element_16 import LinksElement16
from adyen.models.links_element_17 import LinksElement17
from adyen.models.links_element_19 import LinksElement19


class WebhookLinks(object):
    """Implementation of the 'WebhookLinks' model.

    Attributes:
        company (LinksElement15): The company account that the webhook is configured
            for. Only present for company-level webhooks.
        generate_hmac (LinksElement16): Generate an HMAC key.
        merchant (LinksElement17): The merchant account that the webhook is
            configured for. Only present for merchant-level webhooks.
        mself (LinksElement6): Link to the resource itself.
        test_webhook (LinksElement19): Test the webhook setup.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "generate_hmac": "generateHmac",
        "mself": "self",
        "test_webhook": "testWebhook",
        "company": "company",
        "merchant": "merchant",
    }

    _optionals = [
        "company",
        "merchant",
    ]

    def __init__(
        self,
        generate_hmac=None,
        mself=None,
        test_webhook=None,
        company=APIHelper.SKIP,
        merchant=APIHelper.SKIP):
        """Initialize a WebhookLinks instance."""
        # Initialize members of the class
        if company is not APIHelper.SKIP:
            self.company = company
        self.generate_hmac = generate_hmac
        if merchant is not APIHelper.SKIP:
            self.merchant = merchant
        self.mself = mself
        self.test_webhook = test_webhook

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        generate_hmac =\
            LinksElement16.from_dictionary(
                dictionary.get("generateHmac"))\
                if dictionary.get("generateHmac") else None
        mself =\
            LinksElement6.from_dictionary(
                dictionary.get("self"))\
                if dictionary.get("self") else None
        test_webhook =\
            LinksElement19.from_dictionary(
                dictionary.get("testWebhook"))\
                if dictionary.get("testWebhook") else None
        company =\
            LinksElement15.from_dictionary(
                dictionary.get("company"))\
                if "company" in dictionary.keys()\
                else APIHelper.SKIP
        merchant =\
            LinksElement17.from_dictionary(
                dictionary.get("merchant"))\
                if "merchant" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(generate_hmac,
                   mself,
                   test_webhook,
                   company,
                   merchant)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _company=(
            self.company
            if hasattr(self, "company")
            else None
        )
        _generate_hmac=self.generate_hmac
        _merchant=(
            self.merchant
            if hasattr(self, "merchant")
            else None
        )
        _mself=self.mself
        _test_webhook=self.test_webhook
        return (
            f"{self.__class__.__name__}("
            f"company={_company!r}, "
            f"generate_hmac={_generate_hmac!r}, "
            f"merchant={_merchant!r}, "
            f"mself={_mself!r}, "
            f"test_webhook={_test_webhook!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _company=(
            self.company
            if hasattr(self, "company")
            else None
        )
        _generate_hmac=self.generate_hmac
        _merchant=(
            self.merchant
            if hasattr(self, "merchant")
            else None
        )
        _mself=self.mself
        _test_webhook=self.test_webhook
        return (
            f"{self.__class__.__name__}("
            f"company={_company!s}, "
            f"generate_hmac={_generate_hmac!s}, "
            f"merchant={_merchant!s}, "
            f"mself={_mself!s}, "
            f"test_webhook={_test_webhook!s}, "
            f")"
        )
