"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
class NotificationEventConfiguration(object):
    """Implementation of the 'NotificationEventConfiguration' model.

    Attributes:
        event_type (EventTypeEnum): The type of event.  Possible values:
            **ACCOUNT_CLOSED**, **ACCOUNT_CREATED**,
            **ACCOUNT_FUNDS_BELOW_THRESHOLD**, **ACCOUNT_HOLDER_CREATED**,
            **ACCOUNT_HOLDER_LIMIT_REACHED**, **ACCOUNT_HOLDER_PAYOUT**,
            **ACCOUNT_HOLDER_STATUS_CHANGE**, **ACCOUNT_HOLDER_STORE_STATUS_CHANGE**,
            **ACCOUNT_HOLDER_UPCOMING_DEADLINE**, **ACCOUNT_HOLDER_UPDATED**,
            **ACCOUNT_HOLDER_VERIFICATION**, **ACCOUNT_UPDATED**,
            **BENEFICIARY_SETUP**, **COMPENSATE_NEGATIVE_BALANCE**,
            **DIRECT_DEBIT_INITIATED**, **PAYMENT_FAILURE**,
            **REFUND_FUNDS_TRANSFER**, **REPORT_AVAILABLE**, **SCHEDULED_REFUNDS**,
            **TRANSFER_FUNDS**.
        include_mode (IncludeModeEnum): Indicates whether the specified `eventType`
            is sent to your webhook endpoint. Possible values: * **INCLUDE**: Send
            the specified `eventType`. * **EXCLUDE**: Send all event types except the
            specified `eventType` and other event types with the `includeMode` set to
            **EXCLUDE**.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "event_type": "eventType",
        "include_mode": "includeMode",
    }

    def __init__(
        self,
        event_type=None,
        include_mode=None):
        """Initialize a NotificationEventConfiguration instance."""
        # Initialize members of the class
        self.event_type = event_type
        self.include_mode = include_mode

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        event_type =\
            dictionary.get("eventType")\
            if dictionary.get("eventType")\
                else None
        include_mode =\
            dictionary.get("includeMode")\
            if dictionary.get("includeMode")\
                else None

        # Return an object of this model
        return cls(event_type,
                   include_mode)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _event_type=self.event_type
        _include_mode=self.include_mode
        return (
            f"{self.__class__.__name__}("
            f"event_type={_event_type!r}, "
            f"include_mode={_include_mode!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _event_type=self.event_type
        _include_mode=self.include_mode
        return (
            f"{self.__class__.__name__}("
            f"event_type={_event_type!s}, "
            f"include_mode={_include_mode!s}, "
            f")"
        )
