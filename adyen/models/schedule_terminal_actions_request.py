"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper


class ScheduleTerminalActionsRequest(object):
    """Implementation of the 'ScheduleTerminalActionsRequest' model.

    Attributes:
        action_details (ForceRebootDetails | InstallAndroidAppDetails |
            InstallAndroidCertificateDetails | ReleaseUpdateDetails |
            UninstallAndroidAppDetails | UninstallAndroidCertificateDetails | None):
            Information about the action to take.
        scheduled_at (str): The date and time when the action should happen.  Format:
            [RFC 3339](https://www.rfc-editor.org/rfc/rfc3339), but without the **Z**
            before the time offset. For example, **2021-11-15T12:16:21+0100**  The
            action is sent with the first [maintenance
            call](https://docs.adyen.com/point-of-sale/automating-terminal-management/
            terminal-actions-api#when-actions-take-effect) after the specified date
            and time in the time zone of the terminal.  An empty value causes the
            action to be sent as soon as possible: at the next maintenance call.
        store_id (str): The unique ID of the
            [store](https://docs.adyen.com/api-explorer/#/ManagementService/latest/get
            /stores). If present, all terminals in the `terminalIds` list must be
            assigned to this store.
        terminal_ids (List[str]): A list of unique IDs of the terminals to apply the
            action to. You can extract the IDs from the [GET
            `/terminals`](https://docs.adyen.com/api-explorer/#/ManagementService/late
            st/get/terminals) response. Maximum length: 100 IDs.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "action_details": "actionDetails",
        "scheduled_at": "scheduledAt",
        "store_id": "storeId",
        "terminal_ids": "terminalIds",
    }

    _optionals = [
        "action_details",
        "scheduled_at",
        "store_id",
        "terminal_ids",
    ]

    def __init__(
        self,
        action_details=APIHelper.SKIP,
        scheduled_at=APIHelper.SKIP,
        store_id=APIHelper.SKIP,
        terminal_ids=APIHelper.SKIP):
        """Initialize a ScheduleTerminalActionsRequest instance."""
        # Initialize members of the class
        if action_details is not APIHelper.SKIP:
            self.action_details = action_details
        if scheduled_at is not APIHelper.SKIP:
            self.scheduled_at = scheduled_at
        if store_id is not APIHelper.SKIP:
            self.store_id = store_id
        if terminal_ids is not APIHelper.SKIP:
            self.terminal_ids = terminal_ids

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        from adyen.utilities.union_type_lookup import (
            UnionTypeLookUp,
        )

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        action_details = APIHelper.deserialize_union_type(
            UnionTypeLookUp.get("ScheduleTerminalActionsRequestActionDetails"),
            dictionary.get("actionDetails"),
            False)\
            if dictionary.get("actionDetails") is not None\
            else APIHelper.SKIP
        scheduled_at =\
            dictionary.get("scheduledAt")\
            if dictionary.get("scheduledAt")\
                else APIHelper.SKIP
        store_id =\
            dictionary.get("storeId")\
            if dictionary.get("storeId")\
                else APIHelper.SKIP
        terminal_ids =\
            dictionary.get("terminalIds")\
            if dictionary.get("terminalIds")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(action_details,
                   scheduled_at,
                   store_id,
                   terminal_ids)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _action_details=(
            self.action_details
            if hasattr(self, "action_details")
            else None
        )
        _scheduled_at=(
            self.scheduled_at
            if hasattr(self, "scheduled_at")
            else None
        )
        _store_id=(
            self.store_id
            if hasattr(self, "store_id")
            else None
        )
        _terminal_ids=(
            self.terminal_ids
            if hasattr(self, "terminal_ids")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"action_details={_action_details!r}, "
            f"scheduled_at={_scheduled_at!r}, "
            f"store_id={_store_id!r}, "
            f"terminal_ids={_terminal_ids!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _action_details=(
            self.action_details
            if hasattr(self, "action_details")
            else None
        )
        _scheduled_at=(
            self.scheduled_at
            if hasattr(self, "scheduled_at")
            else None
        )
        _store_id=(
            self.store_id
            if hasattr(self, "store_id")
            else None
        )
        _terminal_ids=(
            self.terminal_ids
            if hasattr(self, "terminal_ids")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"action_details={_action_details!s}, "
            f"scheduled_at={_scheduled_at!s}, "
            f"store_id={_store_id!s}, "
            f"terminal_ids={_terminal_ids!s}, "
            f")"
        )
