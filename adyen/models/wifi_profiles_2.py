"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.profile import Profile
from adyen.models.settings_1 import Settings1


class WifiProfiles2(object):
    """Implementation of the 'WifiProfiles2' model.

    Remote Wi-Fi profiles for WPA and WPA2 PSK and EAP Wi-Fi networks.

    Attributes:
        profiles (List[Profile]): List of remote Wi-Fi profiles.
        settings (Settings1): General Wi-Fi settings.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "profiles": "profiles",
        "settings": "settings",
    }

    _optionals = [
        "profiles",
        "settings",
    ]

    def __init__(
        self,
        profiles=APIHelper.SKIP,
        settings=APIHelper.SKIP):
        """Initialize a WifiProfiles2 instance."""
        # Initialize members of the class
        if profiles is not APIHelper.SKIP:
            self.profiles = profiles
        if settings is not APIHelper.SKIP:
            self.settings = settings

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        profiles = None
        if dictionary.get("profiles") is not None:
            profiles = [
                Profile.from_dictionary(x)
                    for x in dictionary.get("profiles")
            ]
        else:
            profiles = APIHelper.SKIP
        settings =\
            Settings1.from_dictionary(
                dictionary.get("settings"))\
                if "settings" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(profiles,
                   settings)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _profiles=(
            self.profiles
            if hasattr(self, "profiles")
            else None
        )
        _settings=(
            self.settings
            if hasattr(self, "settings")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"profiles={_profiles!r}, "
            f"settings={_settings!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _profiles=(
            self.profiles
            if hasattr(self, "profiles")
            else None
        )
        _settings=(
            self.settings
            if hasattr(self, "settings")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"profiles={_profiles!s}, "
            f"settings={_settings!s}, "
            f")"
        )
