"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.patchable_amount_dto_1 import (
    PatchableAmountDTO1,
)


class PatchableTrigger2(object):
    """Implementation of the 'PatchableTrigger2' model.

    The condition that triggers the top-up. This can be a recurring schedule or a
    minimum balance threshold.

    Attributes:
        schedule (PatchableSchedule | None): Contains the details about the schedule
            that determines when the top up is executed.
        threshold (PatchableAmountDTO1): The balance threshold that triggers the
            top-up. If the balance falls below this amount, a top-up is initiated.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "schedule": "schedule",
        "threshold": "threshold",
    }

    _optionals = [
        "schedule",
        "threshold",
    ]

    _nullables = [
        "schedule",
    ]

    def __init__(
        self,
        schedule=APIHelper.SKIP,
        threshold=APIHelper.SKIP):
        """Initialize a PatchableTrigger2 instance."""
        # Initialize members of the class
        if schedule is not APIHelper.SKIP:
            self.schedule = schedule
        if threshold is not APIHelper.SKIP:
            self.threshold = threshold

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        from adyen.utilities.union_type_lookup import (
            UnionTypeLookUp,
        )

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        if "schedule" in dictionary.keys():
            schedule = APIHelper.deserialize_union_type(
            UnionTypeLookUp.get("PatchableTrigger2Schedule"),
            dictionary.get("schedule"),
            False)\
            if dictionary.get("schedule") is not None\
            else None
        else:
            schedule = APIHelper.SKIP
        threshold =\
            PatchableAmountDTO1.from_dictionary(
                dictionary.get("threshold"))\
                if "threshold" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(schedule,
                   threshold)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _schedule=(
            self.schedule
            if hasattr(self, "schedule")
            else None
        )
        _threshold=(
            self.threshold
            if hasattr(self, "threshold")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"schedule={_schedule!r}, "
            f"threshold={_threshold!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _schedule=(
            self.schedule
            if hasattr(self, "schedule")
            else None
        )
        _threshold=(
            self.threshold
            if hasattr(self, "threshold")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"schedule={_schedule!s}, "
            f"threshold={_threshold!s}, "
            f")"
        )
