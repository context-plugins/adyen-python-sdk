"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.first import First
from adyen.models.last import Last
from adyen.models.mself import Self
from adyen.models.next import Next
from adyen.models.prev import Prev


class PaginationLinks1(object):
    """Implementation of the 'PaginationLinks1' model.

    Pagination references.

    Attributes:
        first (First): The model property of type First.
        last (Last): The model property of type Last.
        next (Next): The model property of type Next.
        prev (Prev): The model property of type Prev.
        mself (Self): The model property of type Self.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "first": "first",
        "last": "last",
        "mself": "self",
        "next": "next",
        "prev": "prev",
    }

    _optionals = [
        "next",
        "prev",
    ]

    def __init__(
        self,
        first=None,
        last=None,
        mself=None,
        next=APIHelper.SKIP,
        prev=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a PaginationLinks1 instance."""
        # Initialize members of the class
        self.first = first
        self.last = last
        if next is not APIHelper.SKIP:
            self.next = next
        if prev is not APIHelper.SKIP:
            self.prev = prev
        self.mself = mself

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        first =\
            First.from_dictionary(
                dictionary.get("first"))\
                if dictionary.get("first") else None
        last =\
            Last.from_dictionary(
                dictionary.get("last"))\
                if dictionary.get("last") else None
        mself =\
            Self.from_dictionary(
                dictionary.get("self"))\
                if dictionary.get("self") else None
        next =\
            Next.from_dictionary(
                dictionary.get("next"))\
                if "next" in dictionary.keys()\
                else APIHelper.SKIP
        prev =\
            Prev.from_dictionary(
                dictionary.get("prev"))\
                if "prev" in dictionary.keys()\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(first,
                   last,
                   mself,
                   next,
                   prev,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _first=self.first
        _last=self.last
        _next=(
            self.next
            if hasattr(self, "next")
            else None
        )
        _prev=(
            self.prev
            if hasattr(self, "prev")
            else None
        )
        _mself=self.mself
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"first={_first!r}, "
            f"last={_last!r}, "
            f"next={_next!r}, "
            f"prev={_prev!r}, "
            f"mself={_mself!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _first=self.first
        _last=self.last
        _next=(
            self.next
            if hasattr(self, "next")
            else None
        )
        _prev=(
            self.prev
            if hasattr(self, "prev")
            else None
        )
        _mself=self.mself
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"first={_first!s}, "
            f"last={_last!s}, "
            f"next={_next!s}, "
            f"prev={_prev!s}, "
            f"mself={_mself!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
