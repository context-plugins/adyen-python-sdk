"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.minor_units_monetary_value import (
    MinorUnitsMonetaryValue,
)
from adyen.models.supported_card_types import (
    SupportedCardTypes,
)


class StoreAndForward1(object):
    """Implementation of the 'StoreAndForward1' model.

    Settings for store-and-forward offline payments. The `maxAmount`, `maxPayments`,
    and `supportedCardTypes` parameters must be configured, either in the request or
    inherited from a higher level in your account structure.

    Attributes:
        max_amount (List[MinorUnitsMonetaryValue]): The maximum amount that the
            terminal accepts for a single store-and-forward payment.
        max_payments (int): The maximum number of store-and-forward transactions per
            terminal that you can process while offline.
        supported_card_types (SupportedCardTypes): The model property of type
            SupportedCardTypes.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "max_amount": "maxAmount",
        "max_payments": "maxPayments",
        "supported_card_types": "supportedCardTypes",
    }

    _optionals = [
        "max_amount",
        "max_payments",
        "supported_card_types",
    ]

    def __init__(
        self,
        max_amount=APIHelper.SKIP,
        max_payments=APIHelper.SKIP,
        supported_card_types=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a StoreAndForward1 instance."""
        # Initialize members of the class
        if max_amount is not APIHelper.SKIP:
            self.max_amount = max_amount
        if max_payments is not APIHelper.SKIP:
            self.max_payments = max_payments
        if supported_card_types is not APIHelper.SKIP:
            self.supported_card_types = supported_card_types

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        max_amount = None
        if dictionary.get("maxAmount") is not None:
            max_amount = [
                MinorUnitsMonetaryValue.from_dictionary(x)
                    for x in dictionary.get("maxAmount")
            ]
        else:
            max_amount = APIHelper.SKIP
        max_payments =\
            dictionary.get("maxPayments")\
            if dictionary.get("maxPayments")\
                else APIHelper.SKIP
        supported_card_types =\
            SupportedCardTypes.from_dictionary(
                dictionary.get("supportedCardTypes"))\
                if "supportedCardTypes" in dictionary.keys()\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(max_amount,
                   max_payments,
                   supported_card_types,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _max_amount=(
            self.max_amount
            if hasattr(self, "max_amount")
            else None
        )
        _max_payments=(
            self.max_payments
            if hasattr(self, "max_payments")
            else None
        )
        _supported_card_types=(
            self.supported_card_types
            if hasattr(self, "supported_card_types")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"max_amount={_max_amount!r}, "
            f"max_payments={_max_payments!r}, "
            f"supported_card_types={_supported_card_types!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _max_amount=(
            self.max_amount
            if hasattr(self, "max_amount")
            else None
        )
        _max_payments=(
            self.max_payments
            if hasattr(self, "max_payments")
            else None
        )
        _supported_card_types=(
            self.supported_card_types
            if hasattr(self, "supported_card_types")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"max_amount={_max_amount!s}, "
            f"max_payments={_max_payments!s}, "
            f"supported_card_types={_supported_card_types!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
