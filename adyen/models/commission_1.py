"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper


class Commission1(object):
    """Implementation of the 'Commission1' model.

    Defines your platform's commission for the processed payments as a fixed amount
    (specified in minor units), a percentage (specified in basis points), or both.
    The commission is booked to your platform's liable balance account.

    Attributes:
        fixed_amount (int): A fixed commission fee, in minor units.
        variable_percentage (int): A variable commission fee, in basis points.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "fixed_amount": "fixedAmount",
        "variable_percentage": "variablePercentage",
    }

    _optionals = [
        "fixed_amount",
        "variable_percentage",
    ]

    def __init__(
        self,
        fixed_amount=APIHelper.SKIP,
        variable_percentage=APIHelper.SKIP):
        """Initialize a Commission1 instance."""
        # Initialize members of the class
        if fixed_amount is not APIHelper.SKIP:
            self.fixed_amount = fixed_amount
        if variable_percentage is not APIHelper.SKIP:
            self.variable_percentage = variable_percentage

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        fixed_amount =\
            dictionary.get("fixedAmount")\
            if dictionary.get("fixedAmount")\
                else APIHelper.SKIP
        variable_percentage =\
            dictionary.get("variablePercentage")\
            if dictionary.get("variablePercentage")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(fixed_amount,
                   variable_percentage)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _fixed_amount=(
            self.fixed_amount
            if hasattr(self, "fixed_amount")
            else None
        )
        _variable_percentage=(
            self.variable_percentage
            if hasattr(self, "variable_percentage")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"fixed_amount={_fixed_amount!r}, "
            f"variable_percentage={_variable_percentage!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _fixed_amount=(
            self.fixed_amount
            if hasattr(self, "fixed_amount")
            else None
        )
        _variable_percentage=(
            self.variable_percentage
            if hasattr(self, "variable_percentage")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"fixed_amount={_fixed_amount!s}, "
            f"variable_percentage={_variable_percentage!s}, "
            f")"
        )
