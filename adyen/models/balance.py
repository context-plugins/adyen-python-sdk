"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper


class Balance(object):
    """Implementation of the 'Balance' model.

    Attributes:
        available (int): The balance available for use.
        balance (int): The sum of the transactions that have already been settled.
        currency (str): The three-character [ISO currency
            code](https://docs.adyen.com/development-resources/currency-codes) of the
            balance.
        pending (int): The sum of the transactions that will be settled in the future.
        pending_available (int): The balance that will become the available balance
            after the pending balance is settled.  The pending available balance is
            equal to the lower of the following: - The `pending` balance - The
            `pending` balance plus the `available` balance.
        reserved (int): The balance currently held in reserve.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "available": "available",
        "balance": "balance",
        "currency": "currency",
        "reserved": "reserved",
        "pending": "pending",
        "pending_available": "pendingAvailable",
    }

    _optionals = [
        "pending",
        "pending_available",
    ]

    def __init__(
        self,
        available=None,
        balance=None,
        currency=None,
        reserved=None,
        pending=APIHelper.SKIP,
        pending_available=APIHelper.SKIP):
        """Initialize a Balance instance."""
        # Initialize members of the class
        self.available = available
        self.balance = balance
        self.currency = currency
        if pending is not APIHelper.SKIP:
            self.pending = pending
        if pending_available is not APIHelper.SKIP:
            self.pending_available = pending_available
        self.reserved = reserved

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        available =\
            dictionary.get("available")\
            if dictionary.get("available")\
                else None
        balance =\
            dictionary.get("balance")\
            if dictionary.get("balance")\
                else None
        currency =\
            dictionary.get("currency")\
            if dictionary.get("currency")\
                else None
        reserved =\
            dictionary.get("reserved")\
            if dictionary.get("reserved")\
                else None
        pending =\
            dictionary.get("pending")\
            if dictionary.get("pending")\
                else APIHelper.SKIP
        pending_available =\
            dictionary.get("pendingAvailable")\
            if dictionary.get("pendingAvailable")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(available,
                   balance,
                   currency,
                   reserved,
                   pending,
                   pending_available)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _available=self.available
        _balance=self.balance
        _currency=self.currency
        _pending=(
            self.pending
            if hasattr(self, "pending")
            else None
        )
        _pending_available=(
            self.pending_available
            if hasattr(self, "pending_available")
            else None
        )
        _reserved=self.reserved
        return (
            f"{self.__class__.__name__}("
            f"available={_available!r}, "
            f"balance={_balance!r}, "
            f"currency={_currency!r}, "
            f"pending={_pending!r}, "
            f"pending_available={_pending_available!r}, "
            f"reserved={_reserved!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _available=self.available
        _balance=self.balance
        _currency=self.currency
        _pending=(
            self.pending
            if hasattr(self, "pending")
            else None
        )
        _pending_available=(
            self.pending_available
            if hasattr(self, "pending_available")
            else None
        )
        _reserved=self.reserved
        return (
            f"{self.__class__.__name__}("
            f"available={_available!s}, "
            f"balance={_balance!s}, "
            f"currency={_currency!s}, "
            f"pending={_pending!s}, "
            f"pending_available={_pending_available!s}, "
            f"reserved={_reserved!s}, "
            f")"
        )
