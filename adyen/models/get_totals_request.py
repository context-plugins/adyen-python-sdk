"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.total_filter_2 import TotalFilter2


class GetTotalsRequest(object):
    """Implementation of the 'GetTotalsRequest' model.

    It conveys information from the Sale System related to the scope and the format
    of the totals to be computed by the POI System.
    Content of the Get Totals Request message.

    Attributes:
        total_details (List[TotalDetailEnum]): Indicates the hierarchical structure
            of the reconciliation result of the Sale to POI reconciliation. Required
            to present totals per value of element included in this cluster (POI
            Terminal, Sale Terminal, Cashier, Shift, TotalsGroupID). Possible values:
            * **OperatorID** * **POIID** * **SaleID** * **ShiftNumber** *
            **TotalsGroupID**
        total_filter (TotalFilter2): Filter to compute the totals. Used for the Get
            Totals, to request totals for a (or a combination of) particular value of
            the POI Terminal, Sale Terminal, Cashier, Shift, or TotalsGroupID.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "total_details": "TotalDetails",
        "total_filter": "TotalFilter",
    }

    _optionals = [
        "total_details",
        "total_filter",
    ]

    def __init__(
        self,
        total_details=APIHelper.SKIP,
        total_filter=APIHelper.SKIP):
        """Initialize a GetTotalsRequest instance."""
        # Initialize members of the class
        if total_details is not APIHelper.SKIP:
            self.total_details = total_details
        if total_filter is not APIHelper.SKIP:
            self.total_filter = total_filter

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        total_details =\
            dictionary.get("TotalDetails")\
            if dictionary.get("TotalDetails")\
                else APIHelper.SKIP
        total_filter =\
            TotalFilter2.from_dictionary(
                dictionary.get("TotalFilter"))\
                if "TotalFilter" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(total_details,
                   total_filter)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _total_details=(
            self.total_details
            if hasattr(self, "total_details")
            else None
        )
        _total_filter=(
            self.total_filter
            if hasattr(self, "total_filter")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"total_details={_total_details!r}, "
            f"total_filter={_total_filter!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _total_details=(
            self.total_details
            if hasattr(self, "total_details")
            else None
        )
        _total_filter=(
            self.total_filter
            if hasattr(self, "total_filter")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"total_details={_total_details!s}, "
            f"total_filter={_total_filter!s}, "
            f")"
        )
