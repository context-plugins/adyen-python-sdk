"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.amount_17 import Amount17


class AmountAdjustment(object):
    """Implementation of the 'AmountAdjustment' model.

    Attributes:
        amount (Amount17): The adjustment amount.
        amount_adjustment_type (AmountAdjustmentTypeEnum): The type of markup that is
            applied to an authorised payment.  Possible values: **exchange**,
            **forexMarkup**, **authHoldReserve**, **atmMarkup**.
        basepoints (int): The basepoints associated with the applied markup.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "amount": "amount",
        "amount_adjustment_type": "amountAdjustmentType",
        "basepoints": "basepoints",
    }

    _optionals = [
        "amount",
        "amount_adjustment_type",
        "basepoints",
    ]

    def __init__(
        self,
        amount=APIHelper.SKIP,
        amount_adjustment_type=APIHelper.SKIP,
        basepoints=APIHelper.SKIP):
        """Initialize a AmountAdjustment instance."""
        # Initialize members of the class
        if amount is not APIHelper.SKIP:
            self.amount = amount
        if amount_adjustment_type is not APIHelper.SKIP:
            self.amount_adjustment_type = amount_adjustment_type
        if basepoints is not APIHelper.SKIP:
            self.basepoints = basepoints

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        amount =\
            Amount17.from_dictionary(
                dictionary.get("amount"))\
                if "amount" in dictionary.keys()\
                else APIHelper.SKIP
        amount_adjustment_type =\
            dictionary.get("amountAdjustmentType")\
            if dictionary.get("amountAdjustmentType")\
                else APIHelper.SKIP
        basepoints =\
            dictionary.get("basepoints")\
            if dictionary.get("basepoints")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(amount,
                   amount_adjustment_type,
                   basepoints)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return True

        if not isinstance(dictionary, dict):
            return False

        return True

    def __repr__(self):
        """Return a unambiguous string representation."""
        _amount=(
            self.amount
            if hasattr(self, "amount")
            else None
        )
        _amount_adjustment_type=(
            self.amount_adjustment_type
            if hasattr(self, "amount_adjustment_type")
            else None
        )
        _basepoints=(
            self.basepoints
            if hasattr(self, "basepoints")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"amount={_amount!r}, "
            f"amount_adjustment_type={_amount_adjustment_type!r}, "
            f"basepoints={_basepoints!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _amount=(
            self.amount
            if hasattr(self, "amount")
            else None
        )
        _amount_adjustment_type=(
            self.amount_adjustment_type
            if hasattr(self, "amount_adjustment_type")
            else None
        )
        _basepoints=(
            self.basepoints
            if hasattr(self, "basepoints")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"amount={_amount!s}, "
            f"amount_adjustment_type={_amount_adjustment_type!s}, "
            f"basepoints={_basepoints!s}, "
            f")"
        )
