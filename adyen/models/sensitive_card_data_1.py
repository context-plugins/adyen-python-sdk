"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.track_data import TrackData


class SensitiveCardData1(object):
    """Implementation of the 'SensitiveCardData1' model.

    Sensitive information related to the payment card, entered or read by the Sale
    System.
    If structure non empty and unprotected.

    Attributes:
        pan (int): Primary Account Number.
        card_seq_numb (int): Card Sequence Number. If EntryMode is File, Keyed, or
            Manual.
        expiry_date (int): Date after which the card cannot be used. If EntryMode is
            File.
        track_data (List[TrackData]): Magnetic track or magnetic ink characters line.
            If EntryMode is MagStripe or RFID .

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "pan": "PAN",
        "card_seq_numb": "CardSeqNumb",
        "expiry_date": "ExpiryDate",
        "track_data": "TrackData",
    }

    _optionals = [
        "pan",
        "card_seq_numb",
        "expiry_date",
        "track_data",
    ]

    def __init__(
        self,
        pan=APIHelper.SKIP,
        card_seq_numb=APIHelper.SKIP,
        expiry_date=APIHelper.SKIP,
        track_data=APIHelper.SKIP):
        """Initialize a SensitiveCardData1 instance."""
        # Initialize members of the class
        if pan is not APIHelper.SKIP:
            self.pan = pan
        if card_seq_numb is not APIHelper.SKIP:
            self.card_seq_numb = card_seq_numb
        if expiry_date is not APIHelper.SKIP:
            self.expiry_date = expiry_date
        if track_data is not APIHelper.SKIP:
            self.track_data = track_data

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        pan =\
            dictionary.get("PAN")\
            if dictionary.get("PAN")\
                else APIHelper.SKIP
        card_seq_numb =\
            dictionary.get("CardSeqNumb")\
            if dictionary.get("CardSeqNumb")\
                else APIHelper.SKIP
        expiry_date =\
            dictionary.get("ExpiryDate")\
            if dictionary.get("ExpiryDate")\
                else APIHelper.SKIP
        track_data = None
        if dictionary.get("TrackData") is not None:
            track_data = [
                TrackData.from_dictionary(x)
                    for x in dictionary.get("TrackData")
            ]
        else:
            track_data = APIHelper.SKIP

        # Return an object of this model
        return cls(pan,
                   card_seq_numb,
                   expiry_date,
                   track_data)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _pan=(
            self.pan
            if hasattr(self, "pan")
            else None
        )
        _card_seq_numb=(
            self.card_seq_numb
            if hasattr(self, "card_seq_numb")
            else None
        )
        _expiry_date=(
            self.expiry_date
            if hasattr(self, "expiry_date")
            else None
        )
        _track_data=(
            self.track_data
            if hasattr(self, "track_data")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"pan={_pan!r}, "
            f"card_seq_numb={_card_seq_numb!r}, "
            f"expiry_date={_expiry_date!r}, "
            f"track_data={_track_data!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _pan=(
            self.pan
            if hasattr(self, "pan")
            else None
        )
        _card_seq_numb=(
            self.card_seq_numb
            if hasattr(self, "card_seq_numb")
            else None
        )
        _expiry_date=(
            self.expiry_date
            if hasattr(self, "expiry_date")
            else None
        )
        _track_data=(
            self.track_data
            if hasattr(self, "track_data")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"pan={_pan!s}, "
            f"card_seq_numb={_card_seq_numb!s}, "
            f"expiry_date={_expiry_date!s}, "
            f"track_data={_track_data!s}, "
            f")"
        )
