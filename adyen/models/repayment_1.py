"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.repayment_term import RepaymentTerm
from adyen.models.threshold_repayment_21 import (
    ThresholdRepayment21,
)


class Repayment1(object):
    """Implementation of the 'Repayment1' model.

    Attributes:
        basis_points (int): The percentage of your user's incoming net volume that is
            deducted for repaying the grant. The percentage expressed in [basis
            points](https://www.investopedia.com/terms/b/basispoint.asp).
        term (RepaymentTerm): Contains information about the time period in which
            your user must repay the total amount of the grant.
        threshold (ThresholdRepayment21): Contains the minimum threshold amount that
            your user must repay every 30-day period.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "basis_points": "basisPoints",
        "term": "term",
        "threshold": "threshold",
    }

    _optionals = [
        "term",
        "threshold",
    ]

    def __init__(
        self,
        basis_points=None,
        term=APIHelper.SKIP,
        threshold=APIHelper.SKIP):
        """Initialize a Repayment1 instance."""
        # Initialize members of the class
        self.basis_points = basis_points
        if term is not APIHelper.SKIP:
            self.term = term
        if threshold is not APIHelper.SKIP:
            self.threshold = threshold

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        basis_points =\
            dictionary.get("basisPoints")\
            if dictionary.get("basisPoints")\
                else None
        term =\
            RepaymentTerm.from_dictionary(
                dictionary.get("term"))\
                if "term" in dictionary.keys()\
                else APIHelper.SKIP
        threshold =\
            ThresholdRepayment21.from_dictionary(
                dictionary.get("threshold"))\
                if "threshold" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(basis_points,
                   term,
                   threshold)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _basis_points=self.basis_points
        _term=(
            self.term
            if hasattr(self, "term")
            else None
        )
        _threshold=(
            self.threshold
            if hasattr(self, "threshold")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"basis_points={_basis_points!r}, "
            f"term={_term!r}, "
            f"threshold={_threshold!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _basis_points=self.basis_points
        _term=(
            self.term
            if hasattr(self, "term")
            else None
        )
        _threshold=(
            self.threshold
            if hasattr(self, "threshold")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"basis_points={_basis_points!s}, "
            f"term={_term!s}, "
            f"threshold={_threshold!s}, "
            f")"
        )
