"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
class AccountIdentification(object):
    """Implementation of the 'AccountIdentification' model.

    Attributes:
        mtype (str): The model property of type str.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "mtype": "type",
    }

    _optionals = [
        "mtype",
    ]

    def __init__(
        self,
        mtype="AccountIdentification"):
        """Initialize a AccountIdentification instance."""
        # Initialize members of the class
        self.mtype = mtype

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        discriminators = {
            "iban": IbanAccountIdentification2.from_dictionary,
        }
        unboxer = discriminators.get(dictionary.get("type"))

        # Delegate unboxing to another function if a discriminator
        # value for a child class is present.
        if unboxer:
            return unboxer(dictionary)

        # Extract variables from the dictionary
        mtype =\
            dictionary.get("type")\
            if dictionary.get("type")\
                else "AccountIdentification"

        # Return an object of this model
        return cls(mtype)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _mtype=(
            self.mtype
            if hasattr(self, "mtype")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"mtype={_mtype!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _mtype=(
            self.mtype
            if hasattr(self, "mtype")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"mtype={_mtype!s}, "
            f")"
        )

class IbanAccountIdentification2(AccountIdentification):
    """Implementation of the 'IbanAccountIdentification2' model.
    NOTE: This class inherits from 'AccountIdentification'.

    Attributes:
        iban (str): The IBAN of the bank account.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "iban": "iban",
        "mtype": "type",
    }

    def __init__(
        self,
        iban=None,
        mtype="iban"):
        """Initialize a IbanAccountIdentification2 instance."""
        # Initialize members of the class
        self.iban = iban

        # Call the constructor for the base class
        super(IbanAccountIdentification2, self).__init__(
            mtype)

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        iban =\
            dictionary.get("iban")\
            if dictionary.get("iban")\
                else None
        mtype =\
            dictionary.get("type")\
            if dictionary.get("type")\
                else "iban"

        # Return an object of this model
        return cls(iban,
                   mtype)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _iban=self.iban
        _base_repr = super().__repr__()
        _base_repr = _base_repr[_base_repr.find("(") + 1:-1]
        return (
            f"{self.__class__.__name__}("
            f"base_repr={_base_repr!r}, "
            f"iban={_iban!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _iban=self.iban
        _base_str = super().__str__()
        _base_str = _base_str[_base_str.find("(") + 1:-1]
        return (
            f"{self.__class__.__name__}("
            f"base_str={_base_str!s}, "
            f"iban={_iban!s}, "
            f")"
        )
