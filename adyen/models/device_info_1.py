"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.phone_info_2 import PhoneInfo2


class DeviceInfo1(object):
    """Implementation of the 'DeviceInfo1' model.

    Contains information about the device used to provision the network token.

    Attributes:
        form_factor (str): The type of device used to provision the network token.
        os_name (str): The operating system of the device used to provision the
            network token.
        phone (PhoneInfo2): The information about the phone number of the device used
            to provision the the network token. This object is conditionally returned
            and is available for up to 24 hours after the provisioning request
            (access to this field requires a specific user role, please contact your
            Adyen representative to request permission).

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "form_factor": "formFactor",
        "os_name": "osName",
        "phone": "phone",
    }

    _optionals = [
        "form_factor",
        "os_name",
        "phone",
    ]

    def __init__(
        self,
        form_factor=APIHelper.SKIP,
        os_name=APIHelper.SKIP,
        phone=APIHelper.SKIP):
        """Initialize a DeviceInfo1 instance."""
        # Initialize members of the class
        if form_factor is not APIHelper.SKIP:
            self.form_factor = form_factor
        if os_name is not APIHelper.SKIP:
            self.os_name = os_name
        if phone is not APIHelper.SKIP:
            self.phone = phone

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        form_factor =\
            dictionary.get("formFactor")\
            if dictionary.get("formFactor")\
                else APIHelper.SKIP
        os_name =\
            dictionary.get("osName")\
            if dictionary.get("osName")\
                else APIHelper.SKIP
        phone =\
            PhoneInfo2.from_dictionary(
                dictionary.get("phone"))\
                if "phone" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(form_factor,
                   os_name,
                   phone)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _form_factor=(
            self.form_factor
            if hasattr(self, "form_factor")
            else None
        )
        _os_name=(
            self.os_name
            if hasattr(self, "os_name")
            else None
        )
        _phone=(
            self.phone
            if hasattr(self, "phone")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"form_factor={_form_factor!r}, "
            f"os_name={_os_name!r}, "
            f"phone={_phone!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _form_factor=(
            self.form_factor
            if hasattr(self, "form_factor")
            else None
        )
        _os_name=(
            self.os_name
            if hasattr(self, "os_name")
            else None
        )
        _phone=(
            self.phone
            if hasattr(self, "phone")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"form_factor={_form_factor!s}, "
            f"os_name={_os_name!s}, "
            f"phone={_phone!s}, "
            f")"
        )
