"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper


class Settings(object):
    """Implementation of the 'Settings' model.

    Attributes:
        band (str): The preferred Wi-Fi band, for use if the terminals support
            multiple bands. Possible values: All, 2.4GHz, 5GHz.
        roaming (bool): Indicates whether roaming is enabled on the terminals.
        timeout (int): The connection time-out in seconds. Minimum value: 0.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "band": "band",
        "roaming": "roaming",
        "timeout": "timeout",
    }

    _optionals = [
        "band",
        "roaming",
        "timeout",
    ]

    def __init__(
        self,
        band=APIHelper.SKIP,
        roaming=APIHelper.SKIP,
        timeout=APIHelper.SKIP):
        """Initialize a Settings instance."""
        # Initialize members of the class
        if band is not APIHelper.SKIP:
            self.band = band
        if roaming is not APIHelper.SKIP:
            self.roaming = roaming
        if timeout is not APIHelper.SKIP:
            self.timeout = timeout

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        band =\
            dictionary.get("band")\
            if dictionary.get("band")\
                else APIHelper.SKIP
        roaming =\
            dictionary.get("roaming")\
            if "roaming" in dictionary.keys()\
                else APIHelper.SKIP
        timeout =\
            dictionary.get("timeout")\
            if dictionary.get("timeout")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(band,
                   roaming,
                   timeout)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _band=(
            self.band
            if hasattr(self, "band")
            else None
        )
        _roaming=(
            self.roaming
            if hasattr(self, "roaming")
            else None
        )
        _timeout=(
            self.timeout
            if hasattr(self, "timeout")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"band={_band!r}, "
            f"roaming={_roaming!r}, "
            f"timeout={_timeout!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _band=(
            self.band
            if hasattr(self, "band")
            else None
        )
        _roaming=(
            self.roaming
            if hasattr(self, "roaming")
            else None
        )
        _timeout=(
            self.timeout
            if hasattr(self, "timeout")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"band={_band!s}, "
            f"roaming={_roaming!s}, "
            f"timeout={_timeout!s}, "
            f")"
        )
