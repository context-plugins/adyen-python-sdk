"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
class BankAccountModel1(object):
    """Implementation of the 'BankAccountModel1' model.

    Contains the business account details.

    Attributes:
        form_factor (FormFactorEnum): Business accounts with a `formFactor` value of
            **physical** are business accounts issued under the central bank of that
            country. The default value is **physical** for NL, US, and UK business
            accounts.   Adyen creates a local IBAN for business accounts when the
            `formFactor` value is set to **virtual**. The local IBANs that are
            supported are for DE and FR, which reference a physical NL account, with
            funds being routed through the central bank of NL.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "form_factor": "formFactor",
    }

    _optionals = [
        "form_factor",
    ]

    _nullables = [
        "form_factor",
    ]

    def __init__(
        self,
        form_factor="physical"):
        """Initialize a BankAccountModel1 instance."""
        # Initialize members of the class
        self.form_factor = form_factor

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        form_factor =\
            dictionary.get("formFactor")\
            if dictionary.get("formFactor")\
                else "physical"

        # Return an object of this model
        return cls(form_factor)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _form_factor=(
            self.form_factor
            if hasattr(self, "form_factor")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"form_factor={_form_factor!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _form_factor=(
            self.form_factor
            if hasattr(self, "form_factor")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"form_factor={_form_factor!s}, "
            f")"
        )
