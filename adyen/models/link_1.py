"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.links_element import LinksElement


class Link1(object):
    """Implementation of the 'Link1' model.

    Contains links to the list pages.

    Attributes:
        first (LinksElement): The link to the first page of the list.
        last (LinksElement): The link to the last page of the list.
        next (LinksElement): The link to the next page of the list.
        previous (LinksElement): The link to the previous page of the list.
        mself (LinksElement): The link to the list page you are currently viewing.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "first": "first",
        "last": "last",
        "next": "next",
        "previous": "previous",
        "mself": "self",
    }

    _optionals = [
        "first",
        "last",
        "next",
        "previous",
        "mself",
    ]

    def __init__(
        self,
        first=APIHelper.SKIP,
        last=APIHelper.SKIP,
        next=APIHelper.SKIP,
        previous=APIHelper.SKIP,
        mself=APIHelper.SKIP):
        """Initialize a Link1 instance."""
        # Initialize members of the class
        if first is not APIHelper.SKIP:
            self.first = first
        if last is not APIHelper.SKIP:
            self.last = last
        if next is not APIHelper.SKIP:
            self.next = next
        if previous is not APIHelper.SKIP:
            self.previous = previous
        if mself is not APIHelper.SKIP:
            self.mself = mself

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        first =\
            LinksElement.from_dictionary(
                dictionary.get("first"))\
                if "first" in dictionary.keys()\
                else APIHelper.SKIP
        last =\
            LinksElement.from_dictionary(
                dictionary.get("last"))\
                if "last" in dictionary.keys()\
                else APIHelper.SKIP
        next =\
            LinksElement.from_dictionary(
                dictionary.get("next"))\
                if "next" in dictionary.keys()\
                else APIHelper.SKIP
        previous =\
            LinksElement.from_dictionary(
                dictionary.get("previous"))\
                if "previous" in dictionary.keys()\
                else APIHelper.SKIP
        mself =\
            LinksElement.from_dictionary(
                dictionary.get("self"))\
                if "self" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(first,
                   last,
                   next,
                   previous,
                   mself)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _first=(
            self.first
            if hasattr(self, "first")
            else None
        )
        _last=(
            self.last
            if hasattr(self, "last")
            else None
        )
        _next=(
            self.next
            if hasattr(self, "next")
            else None
        )
        _previous=(
            self.previous
            if hasattr(self, "previous")
            else None
        )
        _mself=(
            self.mself
            if hasattr(self, "mself")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"first={_first!r}, "
            f"last={_last!r}, "
            f"next={_next!r}, "
            f"previous={_previous!r}, "
            f"mself={_mself!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _first=(
            self.first
            if hasattr(self, "first")
            else None
        )
        _last=(
            self.last
            if hasattr(self, "last")
            else None
        )
        _next=(
            self.next
            if hasattr(self, "next")
            else None
        )
        _previous=(
            self.previous
            if hasattr(self, "previous")
            else None
        )
        _mself=(
            self.mself
            if hasattr(self, "mself")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"first={_first!s}, "
            f"last={_last!s}, "
            f"next={_next!s}, "
            f"previous={_previous!s}, "
            f"mself={_mself!s}, "
            f")"
        )
