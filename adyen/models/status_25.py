"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.action_1 import Action1


class Status25(object):
    """Implementation of the 'Status25' model.

    Contains the status of the grant.

    Attributes:
        actions (List[Action1]): A list of actions that need to be completed to
            proceed with the grant.
        code (CodeEnum): The code for the status of the grant. Possible values: -
            **Pending** - **Active** - **Repaid** - **WrittenOff** - **Failed** -
            **Revoked** - **Requested** - **Reviewing** - **Approved** - **Rejected**
            - **Cancelled**

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "code": "code",
        "actions": "actions",
    }

    _optionals = [
        "actions",
    ]

    def __init__(
        self,
        code=None,
        actions=APIHelper.SKIP):
        """Initialize a Status25 instance."""
        # Initialize members of the class
        if actions is not APIHelper.SKIP:
            self.actions = actions
        self.code = code

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        code =\
            dictionary.get("code")\
            if dictionary.get("code")\
                else None
        actions = None
        if dictionary.get("actions") is not None:
            actions = [
                Action1.from_dictionary(x)
                    for x in dictionary.get("actions")
            ]
        else:
            actions = APIHelper.SKIP

        # Return an object of this model
        return cls(code,
                   actions)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _actions=(
            self.actions
            if hasattr(self, "actions")
            else None
        )
        _code=self.code
        return (
            f"{self.__class__.__name__}("
            f"actions={_actions!r}, "
            f"code={_code!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _actions=(
            self.actions
            if hasattr(self, "actions")
            else None
        )
        _code=self.code
        return (
            f"{self.__class__.__name__}("
            f"actions={_actions!s}, "
            f"code={_code!s}, "
            f")"
        )
