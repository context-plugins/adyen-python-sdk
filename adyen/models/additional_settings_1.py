"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper


class AdditionalSettings1(object):
    """Implementation of the 'AdditionalSettings1' model.

    Additional shopper and transaction information to be included in your [standard
    webhooks](https://docs.adyen.com/development-resources/webhooks/webhook-types/#eve
    nt-codes). Find out more about the available [additional
    settings](https://docs.adyen.com/development-resources/webhooks/additional-setting
    s).

    Attributes:
        include_event_codes (List[str]): Object containing list of event codes for
            which the notification will be sent.
        properties (Dict[str, bool]): Object containing boolean key-value pairs. The
            key can be any [standard webhook additional
            setting](https://docs.adyen.com/development-resources/webhooks/additional-
            settings), and the value indicates if the setting is enabled. For
            example, `includeCaptureDelayHours`: **true** means the standard
            notifications you get will contain the number of hours remaining until
            the payment will be captured.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "include_event_codes": "includeEventCodes",
        "properties": "properties",
    }

    _optionals = [
        "include_event_codes",
        "properties",
    ]

    def __init__(
        self,
        include_event_codes=APIHelper.SKIP,
        properties=APIHelper.SKIP):
        """Initialize a AdditionalSettings1 instance."""
        # Initialize members of the class
        if include_event_codes is not APIHelper.SKIP:
            self.include_event_codes = include_event_codes
        if properties is not APIHelper.SKIP:
            self.properties = properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        include_event_codes =\
            dictionary.get("includeEventCodes")\
            if dictionary.get("includeEventCodes")\
                else APIHelper.SKIP
        properties =\
            dictionary.get("properties")\
            if "properties" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(include_event_codes,
                   properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _include_event_codes=(
            self.include_event_codes
            if hasattr(self, "include_event_codes")
            else None
        )
        _properties=(
            self.properties
            if hasattr(self, "properties")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"include_event_codes={_include_event_codes!r}, "
            f"properties={_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _include_event_codes=(
            self.include_event_codes
            if hasattr(self, "include_event_codes")
            else None
        )
        _properties=(
            self.properties
            if hasattr(self, "properties")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"include_event_codes={_include_event_codes!s}, "
            f"properties={_properties!s}, "
            f")"
        )
