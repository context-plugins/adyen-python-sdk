"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.onboarding_link_settings_2 import (
    OnboardingLinkSettings2,
)


class OnboardingLinkInfo(object):
    """Implementation of the 'OnboardingLinkInfo' model.

    Attributes:
        locale (str): The language that will be used for the page, specified by a
            combination of two letter [ISO
            639-1](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) language
            and [ISO 3166-1
            alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) country codes.
            See possible valuesfor
            [marketplaces](https://docs.adyen.com/marketplaces/onboard-users/hosted#su
            pported-languages) or
            [platforms](https://docs.adyen.com/platforms/onboard-users/hosted#supporte
            d-languages).   If not specified in the request or if the language is not
            supported, the page uses the browser language. If the browser language is
            not supported, the page uses **en-US** by default.
        redirect_url (str): The URL where the user is redirected after they complete
            hosted onboarding.
        settings (OnboardingLinkSettings2): Key-value pairs indicating the settings
            for the hosted onboarding page. The key represents a specific setting.
        theme_id (str): The unique identifier of the hosted onboarding theme.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "locale": "locale",
        "redirect_url": "redirectUrl",
        "settings": "settings",
        "theme_id": "themeId",
    }

    _optionals = [
        "locale",
        "redirect_url",
        "settings",
        "theme_id",
    ]

    def __init__(
        self,
        locale=APIHelper.SKIP,
        redirect_url=APIHelper.SKIP,
        settings=APIHelper.SKIP,
        theme_id=APIHelper.SKIP):
        """Initialize a OnboardingLinkInfo instance."""
        # Initialize members of the class
        if locale is not APIHelper.SKIP:
            self.locale = locale
        if redirect_url is not APIHelper.SKIP:
            self.redirect_url = redirect_url
        if settings is not APIHelper.SKIP:
            self.settings = settings
        if theme_id is not APIHelper.SKIP:
            self.theme_id = theme_id

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        locale =\
            dictionary.get("locale")\
            if dictionary.get("locale")\
                else APIHelper.SKIP
        redirect_url =\
            dictionary.get("redirectUrl")\
            if dictionary.get("redirectUrl")\
                else APIHelper.SKIP
        settings =\
            OnboardingLinkSettings2.from_dictionary(
                dictionary.get("settings"))\
                if "settings" in dictionary.keys()\
                else APIHelper.SKIP
        theme_id =\
            dictionary.get("themeId")\
            if dictionary.get("themeId")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(locale,
                   redirect_url,
                   settings,
                   theme_id)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _locale=(
            self.locale
            if hasattr(self, "locale")
            else None
        )
        _redirect_url=(
            self.redirect_url
            if hasattr(self, "redirect_url")
            else None
        )
        _settings=(
            self.settings
            if hasattr(self, "settings")
            else None
        )
        _theme_id=(
            self.theme_id
            if hasattr(self, "theme_id")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"locale={_locale!r}, "
            f"redirect_url={_redirect_url!r}, "
            f"settings={_settings!r}, "
            f"theme_id={_theme_id!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _locale=(
            self.locale
            if hasattr(self, "locale")
            else None
        )
        _redirect_url=(
            self.redirect_url
            if hasattr(self, "redirect_url")
            else None
        )
        _settings=(
            self.settings
            if hasattr(self, "settings")
            else None
        )
        _theme_id=(
            self.theme_id
            if hasattr(self, "theme_id")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"locale={_locale!s}, "
            f"redirect_url={_redirect_url!s}, "
            f"settings={_settings!s}, "
            f"theme_id={_theme_id!s}, "
            f")"
        )
