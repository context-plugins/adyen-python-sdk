"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.duration_1 import Duration1


class TransactionRuleInterval(object):
    """Implementation of the 'TransactionRuleInterval' model.

    Attributes:
        day_of_month (int): The day of month, used when the `duration.unit` is
            **months**. If not provided, by default, this is set to **1**, the first
            day of the month.
        day_of_week (DayOfWeekEnum): The day of week, used when the `duration.unit`
            is **weeks**. If not provided, by default, this is set to **monday**.
            Possible values: **sunday**, **monday**, **tuesday**, **wednesday**,
            **thursday**, **friday**.
        duration (Duration1): The duration, which you can specify in hours, days,
            weeks, or months. The maximum duration is 90 days or an equivalent in
            other units. Required when the `type` is **rolling** or **sliding**.
        time_of_day (str): The time of day, in **hh:mm:ss** format, used when the
            `duration.unit` is **hours**. If not provided, by default, this is set to
            **00:00:00**.
        time_zone (str): The [time
            zone](https://en.wikipedia.org/wiki/List_of_tz_database_time_zones). For
            example, **Europe/Amsterdam**. By default, this is set to **UTC**.
        mtype (Type131Enum): The [type of
            interval](https://docs.adyen.com/issuing/transaction-rules#time-intervals)
            during which the rule conditions and limits apply, and how often counters
            are reset.  Possible values:   * **perTransaction**: conditions are
            evaluated and the counters are reset for every transaction.  * **daily**:
            the counters are reset daily at 00:00:00 CET.  * **weekly**: the counters
            are reset every Monday at 00:00:00 CET.   * **monthly**: the counters
            reset every first day of the month at 00:00:00 CET.   * **lifetime**:
            conditions are applied to the lifetime of the payment instrument.  *
            **rolling**: conditions are applied and the counters are reset based on a
            `duration`. If the reset date and time are not provided, Adyen applies
            the default reset time similar to fixed intervals. For example, if the
            duration is every two weeks, the counter resets every third Monday at
            00:00:00 CET.  * **sliding**: conditions are applied and the counters are
            reset based on the current time and a `duration` that you specify.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "mtype": "type",
        "day_of_month": "dayOfMonth",
        "day_of_week": "dayOfWeek",
        "duration": "duration",
        "time_of_day": "timeOfDay",
        "time_zone": "timeZone",
    }

    _optionals = [
        "day_of_month",
        "day_of_week",
        "duration",
        "time_of_day",
        "time_zone",
    ]

    def __init__(
        self,
        mtype=None,
        day_of_month=APIHelper.SKIP,
        day_of_week=APIHelper.SKIP,
        duration=APIHelper.SKIP,
        time_of_day=APIHelper.SKIP,
        time_zone=APIHelper.SKIP):
        """Initialize a TransactionRuleInterval instance."""
        # Initialize members of the class
        if day_of_month is not APIHelper.SKIP:
            self.day_of_month = day_of_month
        if day_of_week is not APIHelper.SKIP:
            self.day_of_week = day_of_week
        if duration is not APIHelper.SKIP:
            self.duration = duration
        if time_of_day is not APIHelper.SKIP:
            self.time_of_day = time_of_day
        if time_zone is not APIHelper.SKIP:
            self.time_zone = time_zone
        self.mtype = mtype

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        mtype =\
            dictionary.get("type")\
            if dictionary.get("type")\
                else None
        day_of_month =\
            dictionary.get("dayOfMonth")\
            if dictionary.get("dayOfMonth")\
                else APIHelper.SKIP
        day_of_week =\
            dictionary.get("dayOfWeek")\
            if dictionary.get("dayOfWeek")\
                else APIHelper.SKIP
        duration =\
            Duration1.from_dictionary(
                dictionary.get("duration"))\
                if "duration" in dictionary.keys()\
                else APIHelper.SKIP
        time_of_day =\
            dictionary.get("timeOfDay")\
            if dictionary.get("timeOfDay")\
                else APIHelper.SKIP
        time_zone =\
            dictionary.get("timeZone")\
            if dictionary.get("timeZone")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(mtype,
                   day_of_month,
                   day_of_week,
                   duration,
                   time_of_day,
                   time_zone)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _day_of_month=(
            self.day_of_month
            if hasattr(self, "day_of_month")
            else None
        )
        _day_of_week=(
            self.day_of_week
            if hasattr(self, "day_of_week")
            else None
        )
        _duration=(
            self.duration
            if hasattr(self, "duration")
            else None
        )
        _time_of_day=(
            self.time_of_day
            if hasattr(self, "time_of_day")
            else None
        )
        _time_zone=(
            self.time_zone
            if hasattr(self, "time_zone")
            else None
        )
        _mtype=self.mtype
        return (
            f"{self.__class__.__name__}("
            f"day_of_month={_day_of_month!r}, "
            f"day_of_week={_day_of_week!r}, "
            f"duration={_duration!r}, "
            f"time_of_day={_time_of_day!r}, "
            f"time_zone={_time_zone!r}, "
            f"mtype={_mtype!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _day_of_month=(
            self.day_of_month
            if hasattr(self, "day_of_month")
            else None
        )
        _day_of_week=(
            self.day_of_week
            if hasattr(self, "day_of_week")
            else None
        )
        _duration=(
            self.duration
            if hasattr(self, "duration")
            else None
        )
        _time_of_day=(
            self.time_of_day
            if hasattr(self, "time_of_day")
            else None
        )
        _time_zone=(
            self.time_zone
            if hasattr(self, "time_zone")
            else None
        )
        _mtype=self.mtype
        return (
            f"{self.__class__.__name__}("
            f"day_of_month={_day_of_month!s}, "
            f"day_of_week={_day_of_week!s}, "
            f"duration={_duration!s}, "
            f"time_of_day={_time_of_day!s}, "
            f"time_zone={_time_zone!s}, "
            f"mtype={_mtype!s}, "
            f")"
        )
