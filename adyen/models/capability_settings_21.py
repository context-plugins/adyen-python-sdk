"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.patchable_amount_dto import (
    PatchableAmountDTO,
)


class CapabilitySettings21(object):
    """Implementation of the 'CapabilitySettings21' model.

    The settings that are requested for the legal entity.

    Attributes:
        amount_per_industry (Dict[str, PatchableAmountDTO]): The maximum amount a
            card holder can spend per industry.
        authorized_card_users (bool): The number of card holders who can use the card.
        funding_source (List[FundingSourceEnum]): The funding source of the card, for
            example **debit**.
        interval (IntervalEnum): The period when the rule conditions apply.
        max_amount (PatchableAmountDTO): The maximum amount a card holder can
            withdraw per day.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "amount_per_industry": "amountPerIndustry",
        "authorized_card_users": "authorizedCardUsers",
        "funding_source": "fundingSource",
        "interval": "interval",
        "max_amount": "maxAmount",
    }

    _optionals = [
        "amount_per_industry",
        "authorized_card_users",
        "funding_source",
        "interval",
        "max_amount",
    ]

    def __init__(
        self,
        amount_per_industry=APIHelper.SKIP,
        authorized_card_users=APIHelper.SKIP,
        funding_source=APIHelper.SKIP,
        interval=APIHelper.SKIP,
        max_amount=APIHelper.SKIP):
        """Initialize a CapabilitySettings21 instance."""
        # Initialize members of the class
        if amount_per_industry is not APIHelper.SKIP:
            self.amount_per_industry = amount_per_industry
        if authorized_card_users is not APIHelper.SKIP:
            self.authorized_card_users = authorized_card_users
        if funding_source is not APIHelper.SKIP:
            self.funding_source = funding_source
        if interval is not APIHelper.SKIP:
            self.interval = interval
        if max_amount is not APIHelper.SKIP:
            self.max_amount = max_amount

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        amount_per_industry =\
            PatchableAmountDTO.from_dictionary(
                dictionary.get("amountPerIndustry"))\
                if "amountPerIndustry" in dictionary.keys()\
                else APIHelper.SKIP
        authorized_card_users =\
            dictionary.get("authorizedCardUsers")\
            if "authorizedCardUsers" in dictionary.keys()\
                else APIHelper.SKIP
        funding_source =\
            dictionary.get("fundingSource")\
            if dictionary.get("fundingSource")\
                else APIHelper.SKIP
        interval =\
            dictionary.get("interval")\
            if dictionary.get("interval")\
                else APIHelper.SKIP
        max_amount =\
            PatchableAmountDTO.from_dictionary(
                dictionary.get("maxAmount"))\
                if "maxAmount" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(amount_per_industry,
                   authorized_card_users,
                   funding_source,
                   interval,
                   max_amount)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _amount_per_industry=(
            self.amount_per_industry
            if hasattr(self, "amount_per_industry")
            else None
        )
        _authorized_card_users=(
            self.authorized_card_users
            if hasattr(self, "authorized_card_users")
            else None
        )
        _funding_source=(
            self.funding_source
            if hasattr(self, "funding_source")
            else None
        )
        _interval=(
            self.interval
            if hasattr(self, "interval")
            else None
        )
        _max_amount=(
            self.max_amount
            if hasattr(self, "max_amount")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"amount_per_industry={_amount_per_industry!r}, "
            f"authorized_card_users={_authorized_card_users!r}, "
            f"funding_source={_funding_source!r}, "
            f"interval={_interval!r}, "
            f"max_amount={_max_amount!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _amount_per_industry=(
            self.amount_per_industry
            if hasattr(self, "amount_per_industry")
            else None
        )
        _authorized_card_users=(
            self.authorized_card_users
            if hasattr(self, "authorized_card_users")
            else None
        )
        _funding_source=(
            self.funding_source
            if hasattr(self, "funding_source")
            else None
        )
        _interval=(
            self.interval
            if hasattr(self, "interval")
            else None
        )
        _max_amount=(
            self.max_amount
            if hasattr(self, "max_amount")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"amount_per_industry={_amount_per_industry!s}, "
            f"authorized_card_users={_authorized_card_users!s}, "
            f"funding_source={_funding_source!s}, "
            f"interval={_interval!s}, "
            f"max_amount={_max_amount!s}, "
            f")"
        )
