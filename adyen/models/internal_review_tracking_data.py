"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.status_44_enum import Status44Enum


class InternalReviewTrackingData(object):
    """Implementation of the 'InternalReviewTrackingData' model.

    Attributes:
        reason (Reason11Enum): The reason why the transfer failed Adyen's internal
            review.   Possible values:  - **refusedForRegulatoryReasons**: the
            transfer does not comply with Adyen's risk policy. For more information,
            [contact the Support Team](https://www.adyen.help/hc/en-us/requests/new).
        status (Status44Enum): The status of the transfer.  Possible values:   -
            **pending**: the transfer is under internal review by Adyen.  -
            **failed**: the transfer failed Adyen's internal review. For details, see
            `reason`.
        mtype (str): The type of tracking event.   Possible values:    -
            **internalReview**: the transfer was flagged because it does not comply
            with Adyen's risk policy.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "status": "status",
        "mtype": "type",
        "reason": "reason",
    }

    _optionals = [
        "reason",
    ]

    def __init__(
        self,
        status=None,
        reason=APIHelper.SKIP):
        """Initialize a InternalReviewTrackingData instance."""
        # Initialize members of the class
        if reason is not APIHelper.SKIP:
            self.reason = reason
        self.status = status
        self.mtype = "internalReview"

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        status =\
            dictionary.get("status")\
            if dictionary.get("status")\
                else None
        reason =\
            dictionary.get("reason")\
            if dictionary.get("reason")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(status,
                   reason)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.status,
                    type_callable=lambda value:
                        Status44Enum.validate(value)) \
                and APIHelper.is_valid_type(
                    value=dictionary.mtype,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("status"),
                type_callable=lambda value:
                    Status44Enum.validate(value)) \
            and APIHelper.is_valid_type(
                value=dictionary.get("type"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _reason=(
            self.reason
            if hasattr(self, "reason")
            else None
        )
        _status=self.status
        _mtype=self.mtype
        return (
            f"{self.__class__.__name__}("
            f"reason={_reason!r}, "
            f"status={_status!r}, "
            f"mtype={_mtype!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _reason=(
            self.reason
            if hasattr(self, "reason")
            else None
        )
        _status=self.status
        _mtype=self.mtype
        return (
            f"{self.__class__.__name__}("
            f"reason={_reason!s}, "
            f"status={_status!s}, "
            f"mtype={_mtype!s}, "
            f")"
        )
