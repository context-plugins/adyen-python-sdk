"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper


class CommonField4(object):
    """Implementation of the 'CommonField4' model.

    Adyen-developed software, such as libraries and plugins, used to interact with
    the Adyen API. For example, Magento plugin, Java API library, etc.

    Attributes:
        name (str): Name of the field. For example, Name of External Platform.
        version (str): Version of the field. For example, Version of External
            Platform.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "name": "name",
        "version": "version",
    }

    _optionals = [
        "name",
        "version",
    ]

    def __init__(
        self,
        name=APIHelper.SKIP,
        version=APIHelper.SKIP):
        """Initialize a CommonField4 instance."""
        # Initialize members of the class
        if name is not APIHelper.SKIP:
            self.name = name
        if version is not APIHelper.SKIP:
            self.version = version

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        name =\
            dictionary.get("name")\
            if dictionary.get("name")\
                else APIHelper.SKIP
        version =\
            dictionary.get("version")\
            if dictionary.get("version")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(name,
                   version)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return True

        if not isinstance(dictionary, dict):
            return False

        return True

    def __repr__(self):
        """Return a unambiguous string representation."""
        _name=(
            self.name
            if hasattr(self, "name")
            else None
        )
        _version=(
            self.version
            if hasattr(self, "version")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"name={_name!r}, "
            f"version={_version!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _name=(
            self.name
            if hasattr(self, "name")
            else None
        )
        _version=(
            self.version
            if hasattr(self, "version")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"name={_name!s}, "
            f"version={_version!s}, "
            f")"
        )
