"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper


class AccountVerificationRoutesRequest(object):
    """Implementation of the 'AccountVerificationRoutesRequest' model.

    Attributes:
        country (AccountVerificationCountry2Enum): The location where the third-party
            individual's bank account is registered. Adyen uses this information to
            determine an available open banking provider, and to configure the open
            banking flow for that respective location.
        locale (str): The language to use in the open banking flow UI, specified by a
            combination of a two-letter [ISO
            639-1](https://en.wikipedia.org/wiki/List_of_ISO_639_language_codes)
            language code and an [ISO 3166-1
            alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) country code.
            This information is used to configure the open banking flow with the same
            language for a consistent user experience.  Default value: **en-US**
        redirect_url (str): The URL where Adyen redirects the third-party individual
            after they complete the open banking flow.
        state (str): A value that helps you identify the request in callback
            handling. You can generate this value on a per-session basis to protect
            the callback against Cross-Site Request Forgery (CSRF) attacks. This
            value  must be composed of characters that can be successfully
            URL-encoded.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "country": "country",
        "redirect_url": "redirectUrl",
        "locale": "locale",
        "state": "state",
    }

    _optionals = [
        "locale",
        "state",
    ]

    def __init__(
        self,
        country=None,
        redirect_url=None,
        locale=APIHelper.SKIP,
        state=APIHelper.SKIP):
        """Initialize a AccountVerificationRoutesRequest instance."""
        # Initialize members of the class
        self.country = country
        if locale is not APIHelper.SKIP:
            self.locale = locale
        self.redirect_url = redirect_url
        if state is not APIHelper.SKIP:
            self.state = state

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        country =\
            dictionary.get("country")\
            if dictionary.get("country")\
                else None
        redirect_url =\
            dictionary.get("redirectUrl")\
            if dictionary.get("redirectUrl")\
                else None
        locale =\
            dictionary.get("locale")\
            if dictionary.get("locale")\
                else APIHelper.SKIP
        state =\
            dictionary.get("state")\
            if dictionary.get("state")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(country,
                   redirect_url,
                   locale,
                   state)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _country=self.country
        _locale=(
            self.locale
            if hasattr(self, "locale")
            else None
        )
        _redirect_url=self.redirect_url
        _state=(
            self.state
            if hasattr(self, "state")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"country={_country!r}, "
            f"locale={_locale!r}, "
            f"redirect_url={_redirect_url!r}, "
            f"state={_state!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _country=self.country
        _locale=(
            self.locale
            if hasattr(self, "locale")
            else None
        )
        _redirect_url=self.redirect_url
        _state=(
            self.state
            if hasattr(self, "state")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"country={_country!s}, "
            f"locale={_locale!s}, "
            f"redirect_url={_redirect_url!s}, "
            f"state={_state!s}, "
            f")"
        )
