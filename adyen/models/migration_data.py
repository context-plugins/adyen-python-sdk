"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.migrated_accounts import MigratedAccounts
from adyen.models.migrated_shareholders import (
    MigratedShareholders,
)
from adyen.models.migrated_stores import MigratedStores


class MigrationData(object):
    """Implementation of the 'MigrationData' model.

    Attributes:
        account_holder_id (str): The unique identifier of the account holder in the
            balance platform.
        balance_platform (str): The unique identifier of the balance platfrom to
            which the account holder was migrated.
        migrated (bool): Set to **true** if the account holder has been migrated.
        migrated_accounts (List[MigratedAccounts]): Contains the mapping of virtual
            account codes (classic integration) to the balance account codes (balance
            platform) associated with the migrated account holder.
        migrated_shareholders (List[MigratedShareholders]): Contains the mapping of
            shareholders associated with the migrated legal entities.
        migrated_stores (List[MigratedStores]): Contains the mapping of business
            lines and stores associated with the migrated account holder.
        migration_date (datetime): The date when account holder was migrated.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "account_holder_id": "accountHolderId",
        "balance_platform": "balancePlatform",
        "migrated": "migrated",
        "migrated_accounts": "migratedAccounts",
        "migrated_shareholders": "migratedShareholders",
        "migrated_stores": "migratedStores",
        "migration_date": "migrationDate",
    }

    _optionals = [
        "account_holder_id",
        "balance_platform",
        "migrated",
        "migrated_accounts",
        "migrated_shareholders",
        "migrated_stores",
        "migration_date",
    ]

    def __init__(
        self,
        account_holder_id=APIHelper.SKIP,
        balance_platform=APIHelper.SKIP,
        migrated=APIHelper.SKIP,
        migrated_accounts=APIHelper.SKIP,
        migrated_shareholders=APIHelper.SKIP,
        migrated_stores=APIHelper.SKIP,
        migration_date=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a MigrationData instance."""
        # Initialize members of the class
        if account_holder_id is not APIHelper.SKIP:
            self.account_holder_id = account_holder_id
        if balance_platform is not APIHelper.SKIP:
            self.balance_platform = balance_platform
        if migrated is not APIHelper.SKIP:
            self.migrated = migrated
        if migrated_accounts is not APIHelper.SKIP:
            self.migrated_accounts = migrated_accounts
        if migrated_shareholders is not APIHelper.SKIP:
            self.migrated_shareholders = migrated_shareholders
        if migrated_stores is not APIHelper.SKIP:
            self.migrated_stores = migrated_stores
        if migration_date is not APIHelper.SKIP:
            self.migration_date =\
                 APIHelper.apply_datetime_converter(
                migration_date, APIHelper.RFC3339DateTime)\
                 if migration_date else None

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        account_holder_id =\
            dictionary.get("accountHolderId")\
            if dictionary.get("accountHolderId")\
                else APIHelper.SKIP
        balance_platform =\
            dictionary.get("balancePlatform")\
            if dictionary.get("balancePlatform")\
                else APIHelper.SKIP
        migrated =\
            dictionary.get("migrated")\
            if "migrated" in dictionary.keys()\
                else APIHelper.SKIP
        migrated_accounts = None
        if dictionary.get("migratedAccounts") is not None:
            migrated_accounts = [
                MigratedAccounts.from_dictionary(x)
                    for x in dictionary.get("migratedAccounts")
            ]
        else:
            migrated_accounts = APIHelper.SKIP
        migrated_shareholders = None
        if dictionary.get("migratedShareholders") is not None:
            migrated_shareholders = [
                MigratedShareholders.from_dictionary(x)
                    for x in dictionary.get("migratedShareholders")
            ]
        else:
            migrated_shareholders = APIHelper.SKIP
        migrated_stores = None
        if dictionary.get("migratedStores") is not None:
            migrated_stores = [
                MigratedStores.from_dictionary(x)
                    for x in dictionary.get("migratedStores")
            ]
        else:
            migrated_stores = APIHelper.SKIP
        migration_date = APIHelper.RFC3339DateTime.from_value(
            dictionary.get("migrationDate")).datetime\
            if dictionary.get("migrationDate") else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(account_holder_id,
                   balance_platform,
                   migrated,
                   migrated_accounts,
                   migrated_shareholders,
                   migrated_stores,
                   migration_date,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _account_holder_id=(
            self.account_holder_id
            if hasattr(self, "account_holder_id")
            else None
        )
        _balance_platform=(
            self.balance_platform
            if hasattr(self, "balance_platform")
            else None
        )
        _migrated=(
            self.migrated
            if hasattr(self, "migrated")
            else None
        )
        _migrated_accounts=(
            self.migrated_accounts
            if hasattr(self, "migrated_accounts")
            else None
        )
        _migrated_shareholders=(
            self.migrated_shareholders
            if hasattr(self, "migrated_shareholders")
            else None
        )
        _migrated_stores=(
            self.migrated_stores
            if hasattr(self, "migrated_stores")
            else None
        )
        _migration_date=(
            self.migration_date
            if hasattr(self, "migration_date")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"account_holder_id={_account_holder_id!r}, "
            f"balance_platform={_balance_platform!r}, "
            f"migrated={_migrated!r}, "
            f"migrated_accounts={_migrated_accounts!r}, "
            f"migrated_shareholders={_migrated_shareholders!r}, "
            f"migrated_stores={_migrated_stores!r}, "
            f"migration_date={_migration_date!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _account_holder_id=(
            self.account_holder_id
            if hasattr(self, "account_holder_id")
            else None
        )
        _balance_platform=(
            self.balance_platform
            if hasattr(self, "balance_platform")
            else None
        )
        _migrated=(
            self.migrated
            if hasattr(self, "migrated")
            else None
        )
        _migrated_accounts=(
            self.migrated_accounts
            if hasattr(self, "migrated_accounts")
            else None
        )
        _migrated_shareholders=(
            self.migrated_shareholders
            if hasattr(self, "migrated_shareholders")
            else None
        )
        _migrated_stores=(
            self.migrated_stores
            if hasattr(self, "migrated_stores")
            else None
        )
        _migration_date=(
            self.migration_date
            if hasattr(self, "migration_date")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"account_holder_id={_account_holder_id!s}, "
            f"balance_platform={_balance_platform!s}, "
            f"migrated={_migrated!s}, "
            f"migrated_accounts={_migrated_accounts!s}, "
            f"migrated_shareholders={_migrated_shareholders!s}, "
            f"migrated_stores={_migrated_stores!s}, "
            f"migration_date={_migration_date!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
