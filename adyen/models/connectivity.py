"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.event_url_3 import EventUrl3


class Connectivity(object):
    """Implementation of the 'Connectivity' model.

    Attributes:
        simcard_status (SimcardStatusEnum): Indicates the status of the SIM card in
            the payment terminal. Can be updated and received only at terminal level,
            and only for models that support cellular connectivity.  Possible values:
            * **ACTIVATED**: the SIM card is activated. Cellular connectivity may
            still need to be enabled on the terminal itself, in the **Network**
            settings. * **INVENTORY**: the SIM card is not activated. The terminal
            can't use cellular connectivity.
        terminal_ip_address_url (EventUrl3): The list of local and public URLs to
            send notifications to when using local integrations.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "simcard_status": "simcardStatus",
        "terminal_ip_address_url": "terminalIPAddressURL",
    }

    _optionals = [
        "simcard_status",
        "terminal_ip_address_url",
    ]

    def __init__(
        self,
        simcard_status=APIHelper.SKIP,
        terminal_ip_address_url=APIHelper.SKIP):
        """Initialize a Connectivity instance."""
        # Initialize members of the class
        if simcard_status is not APIHelper.SKIP:
            self.simcard_status = simcard_status
        if terminal_ip_address_url is not APIHelper.SKIP:
            self.terminal_ip_address_url = terminal_ip_address_url

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        simcard_status =\
            dictionary.get("simcardStatus")\
            if dictionary.get("simcardStatus")\
                else APIHelper.SKIP
        terminal_ip_address_url =\
            EventUrl3.from_dictionary(
                dictionary.get("terminalIPAddressURL"))\
                if "terminalIPAddressURL" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(simcard_status,
                   terminal_ip_address_url)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _simcard_status=(
            self.simcard_status
            if hasattr(self, "simcard_status")
            else None
        )
        _terminal_ip_address_url=(
            self.terminal_ip_address_url
            if hasattr(self, "terminal_ip_address_url")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"simcard_status={_simcard_status!r}, "
            f"terminal_ip_address_url={_terminal_ip_address_url!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _simcard_status=(
            self.simcard_status
            if hasattr(self, "simcard_status")
            else None
        )
        _terminal_ip_address_url=(
            self.terminal_ip_address_url
            if hasattr(self, "terminal_ip_address_url")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"simcard_status={_simcard_status!s}, "
            f"terminal_ip_address_url={_terminal_ip_address_url!s}, "
            f")"
        )
