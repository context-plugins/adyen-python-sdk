"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.loyalty_data import LoyaltyData
from adyen.models.payment_data_1 import PaymentData1
from adyen.models.payment_transaction_1 import (
    PaymentTransaction1,
)
from adyen.models.sale_data_1 import SaleData1


class PaymentRequest2(object):
    """Implementation of the 'PaymentRequest2' model.

    Request sent to terminal to initiate payment.
    It conveys Information related to the Payment transaction to process.
    Content of the `PaymentRequest` message.

    Attributes:
        sale_data (SaleData1): Data related to the Sale System.
        payment_transaction (PaymentTransaction1): Data related to the payment and
            loyalty transaction.
        payment_data (PaymentData1): Data related to the payment transaction. If one
            data element is present.
        loyalty_data (List[LoyaltyData]): Data related to a Loyalty program or
            account. Loyalty cards used with the payment transaction and read by the
            Sale System.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "sale_data": "SaleData",
        "payment_transaction": "PaymentTransaction",
        "payment_data": "PaymentData",
        "loyalty_data": "LoyaltyData",
    }

    _optionals = [
        "payment_data",
        "loyalty_data",
    ]

    def __init__(
        self,
        sale_data=None,
        payment_transaction=None,
        payment_data=APIHelper.SKIP,
        loyalty_data=APIHelper.SKIP):
        """Initialize a PaymentRequest2 instance."""
        # Initialize members of the class
        self.sale_data = sale_data
        self.payment_transaction = payment_transaction
        if payment_data is not APIHelper.SKIP:
            self.payment_data = payment_data
        if loyalty_data is not APIHelper.SKIP:
            self.loyalty_data = loyalty_data

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        sale_data =\
            SaleData1.from_dictionary(
                dictionary.get("SaleData"))\
                if dictionary.get("SaleData") else None
        payment_transaction =\
            PaymentTransaction1.from_dictionary(
                dictionary.get("PaymentTransaction"))\
                if dictionary.get("PaymentTransaction") else None
        payment_data =\
            PaymentData1.from_dictionary(
                dictionary.get("PaymentData"))\
                if "PaymentData" in dictionary.keys()\
                else APIHelper.SKIP
        loyalty_data = None
        if dictionary.get("LoyaltyData") is not None:
            loyalty_data = [
                LoyaltyData.from_dictionary(x)
                    for x in dictionary.get("LoyaltyData")
            ]
        else:
            loyalty_data = APIHelper.SKIP

        # Return an object of this model
        return cls(sale_data,
                   payment_transaction,
                   payment_data,
                   loyalty_data)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _sale_data=self.sale_data
        _payment_transaction=self.payment_transaction
        _payment_data=(
            self.payment_data
            if hasattr(self, "payment_data")
            else None
        )
        _loyalty_data=(
            self.loyalty_data
            if hasattr(self, "loyalty_data")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"sale_data={_sale_data!r}, "
            f"payment_transaction={_payment_transaction!r}, "
            f"payment_data={_payment_data!r}, "
            f"loyalty_data={_loyalty_data!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _sale_data=self.sale_data
        _payment_transaction=self.payment_transaction
        _payment_data=(
            self.payment_data
            if hasattr(self, "payment_data")
            else None
        )
        _loyalty_data=(
            self.loyalty_data
            if hasattr(self, "loyalty_data")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"sale_data={_sale_data!s}, "
            f"payment_transaction={_payment_transaction!s}, "
            f"payment_data={_payment_data!s}, "
            f"loyalty_data={_loyalty_data!s}, "
            f")"
        )
