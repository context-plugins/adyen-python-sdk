"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper


class AddressRequirement(object):
    """Implementation of the 'AddressRequirement' model.

    Attributes:
        description (str): Specifies the required address related fields for a
            particular route.
        required_address_fields (List[RequiredAddressFieldEnum]): List of address
            fields.
        mtype (str): **addressRequirement**

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "mtype": "type",
        "description": "description",
        "required_address_fields": "requiredAddressFields",
    }

    _optionals = [
        "description",
        "required_address_fields",
    ]

    def __init__(
        self,
        description=APIHelper.SKIP,
        required_address_fields=APIHelper.SKIP):
        """Initialize a AddressRequirement instance."""
        # Initialize members of the class
        if description is not APIHelper.SKIP:
            self.description = description
        if required_address_fields is not APIHelper.SKIP:
            self.required_address_fields = required_address_fields
        self.mtype = "addressRequirement"

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        description =\
            dictionary.get("description")\
            if dictionary.get("description")\
                else APIHelper.SKIP
        required_address_fields =\
            dictionary.get("requiredAddressFields")\
            if dictionary.get("requiredAddressFields")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(description,
                   required_address_fields)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.mtype,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("type"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _description=(
            self.description
            if hasattr(self, "description")
            else None
        )
        _required_address_fields=(
            self.required_address_fields
            if hasattr(self, "required_address_fields")
            else None
        )
        _mtype=self.mtype
        return (
            f"{self.__class__.__name__}("
            f"description={_description!r}, "
            f"required_address_fields={_required_address_fields!r}, "
            f"mtype={_mtype!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _description=(
            self.description
            if hasattr(self, "description")
            else None
        )
        _required_address_fields=(
            self.required_address_fields
            if hasattr(self, "required_address_fields")
            else None
        )
        _mtype=self.mtype
        return (
            f"{self.__class__.__name__}("
            f"description={_description!s}, "
            f"required_address_fields={_required_address_fields!s}, "
            f"mtype={_mtype!s}, "
            f")"
        )
