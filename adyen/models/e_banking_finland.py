"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper


class EBankingFinland(object):
    """Implementation of the 'E-Banking Finland' model.

    Attributes:
        checkout_attempt_id (str): The checkout attempt identifier.
        issuer (str): The Ebanking Finland issuer value of the shopper's selected
            bank.
        sdk_data (str): Base64-encoded JSON object containing SDK related parameters
            required by the SDK
        mtype (str): **ebanking_FI**

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "mtype": "type",
        "checkout_attempt_id": "checkoutAttemptId",
        "issuer": "issuer",
        "sdk_data": "sdkData",
    }

    _optionals = [
        "checkout_attempt_id",
        "issuer",
        "sdk_data",
    ]

    def __init__(
        self,
        checkout_attempt_id=APIHelper.SKIP,
        issuer=APIHelper.SKIP,
        sdk_data=APIHelper.SKIP):
        """Initialize a EBankingFinland instance."""
        # Initialize members of the class
        if checkout_attempt_id is not APIHelper.SKIP:
            self.checkout_attempt_id = checkout_attempt_id
        if issuer is not APIHelper.SKIP:
            self.issuer = issuer
        if sdk_data is not APIHelper.SKIP:
            self.sdk_data = sdk_data
        self.mtype = "ebanking_FI"

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        checkout_attempt_id =\
            dictionary.get("checkoutAttemptId")\
            if dictionary.get("checkoutAttemptId")\
                else APIHelper.SKIP
        issuer =\
            dictionary.get("issuer")\
            if dictionary.get("issuer")\
                else APIHelper.SKIP
        sdk_data =\
            dictionary.get("sdkData")\
            if dictionary.get("sdkData")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(checkout_attempt_id,
                   issuer,
                   sdk_data)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.mtype,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("type"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _checkout_attempt_id=(
            self.checkout_attempt_id
            if hasattr(self, "checkout_attempt_id")
            else None
        )
        _issuer=(
            self.issuer
            if hasattr(self, "issuer")
            else None
        )
        _sdk_data=(
            self.sdk_data
            if hasattr(self, "sdk_data")
            else None
        )
        _mtype=self.mtype
        return (
            f"{self.__class__.__name__}("
            f"checkout_attempt_id={_checkout_attempt_id!r}, "
            f"issuer={_issuer!r}, "
            f"sdk_data={_sdk_data!r}, "
            f"mtype={_mtype!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _checkout_attempt_id=(
            self.checkout_attempt_id
            if hasattr(self, "checkout_attempt_id")
            else None
        )
        _issuer=(
            self.issuer
            if hasattr(self, "issuer")
            else None
        )
        _sdk_data=(
            self.sdk_data
            if hasattr(self, "sdk_data")
            else None
        )
        _mtype=self.mtype
        return (
            f"{self.__class__.__name__}("
            f"checkout_attempt_id={_checkout_attempt_id!s}, "
            f"issuer={_issuer!s}, "
            f"sdk_data={_sdk_data!s}, "
            f"mtype={_mtype!s}, "
            f")"
        )
