"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.repayment_term import RepaymentTerm
from adyen.models.threshold_repayment_2 import (
    ThresholdRepayment2,
)


class Repayment(object):
    """Implementation of the 'Repayment' model.

    An object containing the details of the 30-day repayment threshold.

    Attributes:
        basis_points (int): The repayment that is deducted daily from incoming net
            volume, in [basis
            points](https://www.investopedia.com/terms/b/basispoint.asp).
        term (RepaymentTerm): An object containing the details of the configuration
            for repayment term.
        threshold (ThresholdRepayment2): An object containing the details of the
            30-day repayment threshold.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "basis_points": "basisPoints",
        "term": "term",
        "threshold": "threshold",
    }

    _optionals = [
        "term",
        "threshold",
    ]

    def __init__(
        self,
        basis_points=None,
        term=APIHelper.SKIP,
        threshold=APIHelper.SKIP):
        """Initialize a Repayment instance."""
        # Initialize members of the class
        self.basis_points = basis_points
        if term is not APIHelper.SKIP:
            self.term = term
        if threshold is not APIHelper.SKIP:
            self.threshold = threshold

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        basis_points =\
            dictionary.get("basisPoints")\
            if dictionary.get("basisPoints")\
                else None
        term =\
            RepaymentTerm.from_dictionary(
                dictionary.get("term"))\
                if "term" in dictionary.keys()\
                else APIHelper.SKIP
        threshold =\
            ThresholdRepayment2.from_dictionary(
                dictionary.get("threshold"))\
                if "threshold" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(basis_points,
                   term,
                   threshold)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _basis_points=self.basis_points
        _term=(
            self.term
            if hasattr(self, "term")
            else None
        )
        _threshold=(
            self.threshold
            if hasattr(self, "threshold")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"basis_points={_basis_points!r}, "
            f"term={_term!r}, "
            f"threshold={_threshold!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _basis_points=self.basis_points
        _term=(
            self.term
            if hasattr(self, "term")
            else None
        )
        _threshold=(
            self.threshold
            if hasattr(self, "threshold")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"basis_points={_basis_points!s}, "
            f"term={_term!s}, "
            f"threshold={_threshold!s}, "
            f")"
        )
