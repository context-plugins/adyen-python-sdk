"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.url import Url


class NotificationUrl(object):
    """Implementation of the 'NotificationUrl' model.

    Attributes:
        local_urls (List[Url]): One or more local URLs to send notifications to when
            using Terminal API.
        public_urls (List[Url]): One or more public URLs to send notifications to
            when using Terminal API.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "local_urls": "localUrls",
        "public_urls": "publicUrls",
    }

    _optionals = [
        "local_urls",
        "public_urls",
    ]

    def __init__(
        self,
        local_urls=APIHelper.SKIP,
        public_urls=APIHelper.SKIP):
        """Initialize a NotificationUrl instance."""
        # Initialize members of the class
        if local_urls is not APIHelper.SKIP:
            self.local_urls = local_urls
        if public_urls is not APIHelper.SKIP:
            self.public_urls = public_urls

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        local_urls = None
        if dictionary.get("localUrls") is not None:
            local_urls = [
                Url.from_dictionary(x)
                    for x in dictionary.get("localUrls")
            ]
        else:
            local_urls = APIHelper.SKIP
        public_urls = None
        if dictionary.get("publicUrls") is not None:
            public_urls = [
                Url.from_dictionary(x)
                    for x in dictionary.get("publicUrls")
            ]
        else:
            public_urls = APIHelper.SKIP

        # Return an object of this model
        return cls(local_urls,
                   public_urls)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _local_urls=(
            self.local_urls
            if hasattr(self, "local_urls")
            else None
        )
        _public_urls=(
            self.public_urls
            if hasattr(self, "public_urls")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"local_urls={_local_urls!r}, "
            f"public_urls={_public_urls!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _local_urls=(
            self.local_urls
            if hasattr(self, "local_urls")
            else None
        )
        _public_urls=(
            self.public_urls
            if hasattr(self, "public_urls")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"local_urls={_local_urls!s}, "
            f"public_urls={_public_urls!s}, "
            f")"
        )
