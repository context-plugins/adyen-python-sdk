"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper


class TerminalConnectivityEthernet(object):
    """Implementation of the 'TerminalConnectivityEthernet' model.

    Attributes:
        ip_address (str): The terminal's ethernet IP address.
        link_negotiation (str): The ethernet link negotiation that the terminal uses.
        mac_address (str): The terminal's ethernet MAC address.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "ip_address": "ipAddress",
        "link_negotiation": "linkNegotiation",
        "mac_address": "macAddress",
    }

    _optionals = [
        "ip_address",
        "link_negotiation",
        "mac_address",
    ]

    def __init__(
        self,
        ip_address=APIHelper.SKIP,
        link_negotiation=APIHelper.SKIP,
        mac_address=APIHelper.SKIP):
        """Initialize a TerminalConnectivityEthernet instance."""
        # Initialize members of the class
        if ip_address is not APIHelper.SKIP:
            self.ip_address = ip_address
        if link_negotiation is not APIHelper.SKIP:
            self.link_negotiation = link_negotiation
        if mac_address is not APIHelper.SKIP:
            self.mac_address = mac_address

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        ip_address =\
            dictionary.get("ipAddress")\
            if dictionary.get("ipAddress")\
                else APIHelper.SKIP
        link_negotiation =\
            dictionary.get("linkNegotiation")\
            if dictionary.get("linkNegotiation")\
                else APIHelper.SKIP
        mac_address =\
            dictionary.get("macAddress")\
            if dictionary.get("macAddress")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(ip_address,
                   link_negotiation,
                   mac_address)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _ip_address=(
            self.ip_address
            if hasattr(self, "ip_address")
            else None
        )
        _link_negotiation=(
            self.link_negotiation
            if hasattr(self, "link_negotiation")
            else None
        )
        _mac_address=(
            self.mac_address
            if hasattr(self, "mac_address")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"ip_address={_ip_address!r}, "
            f"link_negotiation={_link_negotiation!r}, "
            f"mac_address={_mac_address!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _ip_address=(
            self.ip_address
            if hasattr(self, "ip_address")
            else None
        )
        _link_negotiation=(
            self.link_negotiation
            if hasattr(self, "link_negotiation")
            else None
        )
        _mac_address=(
            self.mac_address
            if hasattr(self, "mac_address")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"ip_address={_ip_address!s}, "
            f"link_negotiation={_link_negotiation!s}, "
            f"mac_address={_mac_address!s}, "
            f")"
        )
