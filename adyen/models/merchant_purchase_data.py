"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.airline_11 import Airline11
from adyen.models.lodging_1 import Lodging1


class MerchantPurchaseData(object):
    """Implementation of the 'MerchantPurchaseData' model.

    Attributes:
        airline (Airline11): Airline information.
        lodging (List[Lodging1]): Lodging information.
        mtype (str): The type of events data.   Possible values:    -
            **merchantPurchaseData**: merchant purchase data

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "mtype": "type",
        "airline": "airline",
        "lodging": "lodging",
    }

    _optionals = [
        "airline",
        "lodging",
    ]

    def __init__(
        self,
        airline=APIHelper.SKIP,
        lodging=APIHelper.SKIP):
        """Initialize a MerchantPurchaseData instance."""
        # Initialize members of the class
        if airline is not APIHelper.SKIP:
            self.airline = airline
        if lodging is not APIHelper.SKIP:
            self.lodging = lodging
        self.mtype = "merchantPurchaseData"

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        airline =\
            Airline11.from_dictionary(
                dictionary.get("airline"))\
                if "airline" in dictionary.keys()\
                else APIHelper.SKIP
        lodging = None
        if dictionary.get("lodging") is not None:
            lodging = [
                Lodging1.from_dictionary(x)
                    for x in dictionary.get("lodging")
            ]
        else:
            lodging = APIHelper.SKIP

        # Return an object of this model
        return cls(airline,
                   lodging)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.mtype,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("type"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _airline=(
            self.airline
            if hasattr(self, "airline")
            else None
        )
        _lodging=(
            self.lodging
            if hasattr(self, "lodging")
            else None
        )
        _mtype=self.mtype
        return (
            f"{self.__class__.__name__}("
            f"airline={_airline!r}, "
            f"lodging={_lodging!r}, "
            f"mtype={_mtype!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _airline=(
            self.airline
            if hasattr(self, "airline")
            else None
        )
        _lodging=(
            self.lodging
            if hasattr(self, "lodging")
            else None
        )
        _mtype=self.mtype
        return (
            f"{self.__class__.__name__}("
            f"airline={_airline!s}, "
            f"lodging={_lodging!s}, "
            f"mtype={_mtype!s}, "
            f")"
        )
