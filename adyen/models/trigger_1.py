"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.amount_17 import Amount17
from adyen.models.schedule_21 import Schedule21


class Trigger1(object):
    """Implementation of the 'Trigger1' model.

    The condition that triggers the top-up. This can be a recurring schedule or a
    minimum balance threshold.

    Attributes:
        schedule (Schedule21): Contains the details about the schedule that
            determines when the top up is executed.
        threshold (Amount17): The balance threshold that triggers the top-up. If the
            balance falls below this amount, a top-up is initiated.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "threshold": "threshold",
        "schedule": "schedule",
    }

    _optionals = [
        "schedule",
    ]

    def __init__(
        self,
        threshold=None,
        schedule=APIHelper.SKIP):
        """Initialize a Trigger1 instance."""
        # Initialize members of the class
        if schedule is not APIHelper.SKIP:
            self.schedule = schedule
        self.threshold = threshold

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        threshold =\
            Amount17.from_dictionary(
                dictionary.get("threshold"))\
                if dictionary.get("threshold") else None
        schedule =\
            Schedule21.from_dictionary(
                dictionary.get("schedule"))\
                if "schedule" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(threshold,
                   schedule)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _schedule=(
            self.schedule
            if hasattr(self, "schedule")
            else None
        )
        _threshold=self.threshold
        return (
            f"{self.__class__.__name__}("
            f"schedule={_schedule!r}, "
            f"threshold={_threshold!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _schedule=(
            self.schedule
            if hasattr(self, "schedule")
            else None
        )
        _threshold=self.threshold
        return (
            f"{self.__class__.__name__}("
            f"schedule={_schedule!s}, "
            f"threshold={_threshold!s}, "
            f")"
        )
