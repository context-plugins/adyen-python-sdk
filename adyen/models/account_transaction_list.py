"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.transaction_1 import Transaction1


class AccountTransactionList(object):
    """Implementation of the 'AccountTransactionList' model.

    Attributes:
        account_code (str): The code of the account.
        has_next_page (bool): Indicates whether there is a next page of transactions
            available.
        transactions (List[Transaction1]): The list of transactions.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "account_code": "accountCode",
        "has_next_page": "hasNextPage",
        "transactions": "transactions",
    }

    _optionals = [
        "account_code",
        "has_next_page",
        "transactions",
    ]

    def __init__(
        self,
        account_code=APIHelper.SKIP,
        has_next_page=APIHelper.SKIP,
        transactions=APIHelper.SKIP):
        """Initialize a AccountTransactionList instance."""
        # Initialize members of the class
        if account_code is not APIHelper.SKIP:
            self.account_code = account_code
        if has_next_page is not APIHelper.SKIP:
            self.has_next_page = has_next_page
        if transactions is not APIHelper.SKIP:
            self.transactions = transactions

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        account_code =\
            dictionary.get("accountCode")\
            if dictionary.get("accountCode")\
                else APIHelper.SKIP
        has_next_page =\
            dictionary.get("hasNextPage")\
            if "hasNextPage" in dictionary.keys()\
                else APIHelper.SKIP
        transactions = None
        if dictionary.get("transactions") is not None:
            transactions = [
                Transaction1.from_dictionary(x)
                    for x in dictionary.get("transactions")
            ]
        else:
            transactions = APIHelper.SKIP

        # Return an object of this model
        return cls(account_code,
                   has_next_page,
                   transactions)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _account_code=(
            self.account_code
            if hasattr(self, "account_code")
            else None
        )
        _has_next_page=(
            self.has_next_page
            if hasattr(self, "has_next_page")
            else None
        )
        _transactions=(
            self.transactions
            if hasattr(self, "transactions")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"account_code={_account_code!r}, "
            f"has_next_page={_has_next_page!r}, "
            f"transactions={_transactions!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _account_code=(
            self.account_code
            if hasattr(self, "account_code")
            else None
        )
        _has_next_page=(
            self.has_next_page
            if hasattr(self, "has_next_page")
            else None
        )
        _transactions=(
            self.transactions
            if hasattr(self, "transactions")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"account_code={_account_code!s}, "
            f"has_next_page={_has_next_page!s}, "
            f"transactions={_transactions!s}, "
            f")"
        )
