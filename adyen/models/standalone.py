"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper


class Standalone(object):
    """Implementation of the 'Standalone' model.

    Attributes:
        currency_code (str): The default currency of the standalone payment terminal
            as an [ISO 4217](https://en.wikipedia.org/wiki/ISO_4217) currency code.
        enable_gratuities (bool): Indicates whether the tipping options specified in
            `gratuities` are enabled on the standalone terminal.
        enable_standalone (bool): Enable standalone mode.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "currency_code": "currencyCode",
        "enable_gratuities": "enableGratuities",
        "enable_standalone": "enableStandalone",
    }

    _optionals = [
        "currency_code",
        "enable_gratuities",
        "enable_standalone",
    ]

    def __init__(
        self,
        currency_code=APIHelper.SKIP,
        enable_gratuities=APIHelper.SKIP,
        enable_standalone=APIHelper.SKIP):
        """Initialize a Standalone instance."""
        # Initialize members of the class
        if currency_code is not APIHelper.SKIP:
            self.currency_code = currency_code
        if enable_gratuities is not APIHelper.SKIP:
            self.enable_gratuities = enable_gratuities
        if enable_standalone is not APIHelper.SKIP:
            self.enable_standalone = enable_standalone

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        currency_code =\
            dictionary.get("currencyCode")\
            if dictionary.get("currencyCode")\
                else APIHelper.SKIP
        enable_gratuities =\
            dictionary.get("enableGratuities")\
            if "enableGratuities" in dictionary.keys()\
                else APIHelper.SKIP
        enable_standalone =\
            dictionary.get("enableStandalone")\
            if "enableStandalone" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(currency_code,
                   enable_gratuities,
                   enable_standalone)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _currency_code=(
            self.currency_code
            if hasattr(self, "currency_code")
            else None
        )
        _enable_gratuities=(
            self.enable_gratuities
            if hasattr(self, "enable_gratuities")
            else None
        )
        _enable_standalone=(
            self.enable_standalone
            if hasattr(self, "enable_standalone")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"currency_code={_currency_code!r}, "
            f"enable_gratuities={_enable_gratuities!r}, "
            f"enable_standalone={_enable_standalone!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _currency_code=(
            self.currency_code
            if hasattr(self, "currency_code")
            else None
        )
        _enable_gratuities=(
            self.enable_gratuities
            if hasattr(self, "enable_gratuities")
            else None
        )
        _enable_standalone=(
            self.enable_standalone
            if hasattr(self, "enable_standalone")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"currency_code={_currency_code!s}, "
            f"enable_gratuities={_enable_gratuities!s}, "
            f"enable_standalone={_enable_standalone!s}, "
            f")"
        )
