"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper


class BalanceMutation(object):
    """Implementation of the 'BalanceMutation' model.

    Attributes:
        balance (int): The amount in the payment's currency that is debited or
            credited on the balance accounting register.
        currency (str): The three-character [ISO currency
            code](https://docs.adyen.com/development-resources/currency-codes).
        received (int): The amount in the payment's currency that is debited or
            credited on the received accounting register.
        reserved (int): The amount in the payment's currency that is debited or
            credited on the reserved accounting register.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "balance": "balance",
        "currency": "currency",
        "received": "received",
        "reserved": "reserved",
    }

    _optionals = [
        "balance",
        "currency",
        "received",
        "reserved",
    ]

    def __init__(
        self,
        balance=APIHelper.SKIP,
        currency=APIHelper.SKIP,
        received=APIHelper.SKIP,
        reserved=APIHelper.SKIP):
        """Initialize a BalanceMutation instance."""
        # Initialize members of the class
        if balance is not APIHelper.SKIP:
            self.balance = balance
        if currency is not APIHelper.SKIP:
            self.currency = currency
        if received is not APIHelper.SKIP:
            self.received = received
        if reserved is not APIHelper.SKIP:
            self.reserved = reserved

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        balance =\
            dictionary.get("balance")\
            if dictionary.get("balance")\
                else APIHelper.SKIP
        currency =\
            dictionary.get("currency")\
            if dictionary.get("currency")\
                else APIHelper.SKIP
        received =\
            dictionary.get("received")\
            if dictionary.get("received")\
                else APIHelper.SKIP
        reserved =\
            dictionary.get("reserved")\
            if dictionary.get("reserved")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(balance,
                   currency,
                   received,
                   reserved)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return True

        if not isinstance(dictionary, dict):
            return False

        return True

    def __repr__(self):
        """Return a unambiguous string representation."""
        _balance=(
            self.balance
            if hasattr(self, "balance")
            else None
        )
        _currency=(
            self.currency
            if hasattr(self, "currency")
            else None
        )
        _received=(
            self.received
            if hasattr(self, "received")
            else None
        )
        _reserved=(
            self.reserved
            if hasattr(self, "reserved")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"balance={_balance!r}, "
            f"currency={_currency!r}, "
            f"received={_received!r}, "
            f"reserved={_reserved!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _balance=(
            self.balance
            if hasattr(self, "balance")
            else None
        )
        _currency=(
            self.currency
            if hasattr(self, "currency")
            else None
        )
        _received=(
            self.received
            if hasattr(self, "received")
            else None
        )
        _reserved=(
            self.reserved
            if hasattr(self, "reserved")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"balance={_balance!s}, "
            f"currency={_currency!s}, "
            f"received={_received!s}, "
            f"reserved={_reserved!s}, "
            f")"
        )
