"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.account_event import AccountEvent
from adyen.models.account_payout_state_2 import (
    AccountPayoutState2,
)
from adyen.models.account_processing_state_2 import (
    AccountProcessingState2,
)


class AccountHolderStatus2(object):
    """Implementation of the 'AccountHolderStatus2' model.

    The status of the new account holder.

    Attributes:
        events (List[AccountEvent]): A list of events scheduled for the account
            holder.
        payout_state (AccountPayoutState2): The payout state of the account holder.
        processing_state (AccountProcessingState2): The processing state of the
            account holder.
        status (Status12Enum): The status of the account holder. >Permitted values:
            `Active`, `Inactive`, `Suspended`, `Closed`.
        status_reason (str): The reason why the status was assigned to the account
            holder.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "status": "status",
        "events": "events",
        "payout_state": "payoutState",
        "processing_state": "processingState",
        "status_reason": "statusReason",
    }

    _optionals = [
        "events",
        "payout_state",
        "processing_state",
        "status_reason",
    ]

    def __init__(
        self,
        status=None,
        events=APIHelper.SKIP,
        payout_state=APIHelper.SKIP,
        processing_state=APIHelper.SKIP,
        status_reason=APIHelper.SKIP):
        """Initialize a AccountHolderStatus2 instance."""
        # Initialize members of the class
        if events is not APIHelper.SKIP:
            self.events = events
        if payout_state is not APIHelper.SKIP:
            self.payout_state = payout_state
        if processing_state is not APIHelper.SKIP:
            self.processing_state = processing_state
        self.status = status
        if status_reason is not APIHelper.SKIP:
            self.status_reason = status_reason

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        status =\
            dictionary.get("status")\
            if dictionary.get("status")\
                else None
        events = None
        if dictionary.get("events") is not None:
            events = [
                AccountEvent.from_dictionary(x)
                    for x in dictionary.get("events")
            ]
        else:
            events = APIHelper.SKIP
        payout_state =\
            AccountPayoutState2.from_dictionary(
                dictionary.get("payoutState"))\
                if "payoutState" in dictionary.keys()\
                else APIHelper.SKIP
        processing_state =\
            AccountProcessingState2.from_dictionary(
                dictionary.get("processingState"))\
                if "processingState" in dictionary.keys()\
                else APIHelper.SKIP
        status_reason =\
            dictionary.get("statusReason")\
            if dictionary.get("statusReason")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(status,
                   events,
                   payout_state,
                   processing_state,
                   status_reason)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _events=(
            self.events
            if hasattr(self, "events")
            else None
        )
        _payout_state=(
            self.payout_state
            if hasattr(self, "payout_state")
            else None
        )
        _processing_state=(
            self.processing_state
            if hasattr(self, "processing_state")
            else None
        )
        _status=self.status
        _status_reason=(
            self.status_reason
            if hasattr(self, "status_reason")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"events={_events!r}, "
            f"payout_state={_payout_state!r}, "
            f"processing_state={_processing_state!r}, "
            f"status={_status!r}, "
            f"status_reason={_status_reason!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _events=(
            self.events
            if hasattr(self, "events")
            else None
        )
        _payout_state=(
            self.payout_state
            if hasattr(self, "payout_state")
            else None
        )
        _processing_state=(
            self.processing_state
            if hasattr(self, "processing_state")
            else None
        )
        _status=self.status
        _status_reason=(
            self.status_reason
            if hasattr(self, "status_reason")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"events={_events!s}, "
            f"payout_state={_payout_state!s}, "
            f"processing_state={_processing_state!s}, "
            f"status={_status!s}, "
            f"status_reason={_status_reason!s}, "
            f")"
        )
