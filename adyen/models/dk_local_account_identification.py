"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper


class DKLocalAccountIdentification(object):
    """Implementation of the 'DKLocalAccountIdentification' model.

    Attributes:
        account_number (str): The 4-10 digits bank account number (Kontonummer)
            (without separators or whitespace).
        bank_code (str): The 4-digit bank code (Registreringsnummer) (without
            separators or whitespace).
        mtype (str): **dkLocal**

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "account_number": "accountNumber",
        "bank_code": "bankCode",
        "mtype": "type",
    }

    def __init__(
        self,
        account_number=None,
        bank_code=None):
        """Initialize a DKLocalAccountIdentification instance."""
        # Initialize members of the class
        self.account_number = account_number
        self.bank_code = bank_code
        self.mtype = "dkLocal"

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        account_number =\
            dictionary.get("accountNumber")\
            if dictionary.get("accountNumber")\
                else None
        bank_code =\
            dictionary.get("bankCode")\
            if dictionary.get("bankCode")\
                else None

        # Return an object of this model
        return cls(account_number,
                   bank_code)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.account_number,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.bank_code,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.mtype,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("accountNumber"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("bankCode"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("type"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _account_number=self.account_number
        _bank_code=self.bank_code
        _mtype=self.mtype
        return (
            f"{self.__class__.__name__}("
            f"account_number={_account_number!r}, "
            f"bank_code={_bank_code!r}, "
            f"mtype={_mtype!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _account_number=self.account_number
        _bank_code=self.bank_code
        _mtype=self.mtype
        return (
            f"{self.__class__.__name__}("
            f"account_number={_account_number!s}, "
            f"bank_code={_bank_code!s}, "
            f"mtype={_mtype!s}, "
            f")"
        )
