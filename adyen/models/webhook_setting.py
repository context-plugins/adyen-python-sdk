"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.condition import Condition
from adyen.models.target_3 import Target3


class WebhookSetting(object):
    """Implementation of the 'WebhookSetting' model.

    Attributes:
        currency (str): The three-character [ISO currency
            code](https://docs.adyen.com/development-resources/currency-codes) of the
            balance.
        id (str): The unique identifier of the webhook setting.
        status (str): The status of the webhook setting. Possible values:  *
            **active**: You receive a balance webhook if any of the conditions in
            this setting are met. * **inactive**: You do not receive a balance
            webhook even if the conditions in this settings are met.
        target (Target3): The resource about whose balance change you want to get
            notified.
        mtype (str): The model property of type str.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "currency": "currency",
        "id": "id",
        "status": "status",
        "target": "target",
        "mtype": "type",
    }

    _optionals = [
        "mtype",
    ]

    def __init__(
        self,
        currency=None,
        id=None,
        status=None,
        target=None,
        mtype="WebhookSetting"):
        """Initialize a WebhookSetting instance."""
        # Initialize members of the class
        self.currency = currency
        self.id = id
        self.status = status
        self.target = target
        self.mtype = mtype

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        discriminators = {
            "balance": BalanceWebhookSetting.from_dictionary,
        }
        unboxer = discriminators.get(dictionary.get("type"))

        # Delegate unboxing to another function if a discriminator
        # value for a child class is present.
        if unboxer:
            return unboxer(dictionary)

        # Extract variables from the dictionary
        currency =\
            dictionary.get("currency")\
            if dictionary.get("currency")\
                else None
        id =\
            dictionary.get("id")\
            if dictionary.get("id")\
                else None
        status =\
            dictionary.get("status")\
            if dictionary.get("status")\
                else None
        target =\
            Target3.from_dictionary(
                dictionary.get("target"))\
                if dictionary.get("target") else None
        mtype =\
            dictionary.get("type")\
            if dictionary.get("type")\
                else "WebhookSetting"

        # Return an object of this model
        return cls(currency,
                   id,
                   status,
                   target,
                   mtype)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _currency=self.currency
        _id=self.id
        _status=self.status
        _target=self.target
        _mtype=(
            self.mtype
            if hasattr(self, "mtype")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"currency={_currency!r}, "
            f"id={_id!r}, "
            f"status={_status!r}, "
            f"target={_target!r}, "
            f"mtype={_mtype!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _currency=self.currency
        _id=self.id
        _status=self.status
        _target=self.target
        _mtype=(
            self.mtype
            if hasattr(self, "mtype")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"currency={_currency!s}, "
            f"id={_id!s}, "
            f"status={_status!s}, "
            f"target={_target!s}, "
            f"mtype={_mtype!s}, "
            f")"
        )

class BalanceWebhookSetting(WebhookSetting):
    """Implementation of the 'BalanceWebhookSetting' model.
    NOTE: This class inherits from 'WebhookSetting'.

    Attributes:
        conditions (List[Condition]): The list of settings and criteria for
            triggering the [balance
            webhook](https://docs.adyen.com/api-explorer/balance-webhooks/latest/post/
            balanceAccount.balance.updated).

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "currency": "currency",
        "id": "id",
        "status": "status",
        "target": "target",
        "conditions": "conditions",
        "mtype": "type",
    }

    _optionals = [
        "conditions",
    ]
    _optionals.extend(WebhookSetting._optionals)

    def __init__(
        self,
        currency=None,
        id=None,
        status=None,
        target=None,
        conditions=APIHelper.SKIP,
        mtype="balance"):
        """Initialize a BalanceWebhookSetting instance."""
        # Initialize members of the class
        if conditions is not APIHelper.SKIP:
            self.conditions = conditions

        # Call the constructor for the base class
        super(BalanceWebhookSetting, self).__init__(
            currency,
            id,
            status,
            target,
            mtype)

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        currency =\
            dictionary.get("currency")\
            if dictionary.get("currency")\
                else None
        id =\
            dictionary.get("id")\
            if dictionary.get("id")\
                else None
        status =\
            dictionary.get("status")\
            if dictionary.get("status")\
                else None
        target =\
            Target3.from_dictionary(
                dictionary.get("target"))\
                if dictionary.get("target") else None
        conditions = None
        if dictionary.get("conditions") is not None:
            conditions = [
                Condition.from_dictionary(x)
                    for x in dictionary.get("conditions")
            ]
        else:
            conditions = APIHelper.SKIP
        mtype =\
            dictionary.get("type")\
            if dictionary.get("type")\
                else "balance"

        # Return an object of this model
        return cls(currency,
                   id,
                   status,
                   target,
                   conditions,
                   mtype)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _conditions=(
            self.conditions
            if hasattr(self, "conditions")
            else None
        )
        _base_repr = super().__repr__()
        _base_repr = _base_repr[_base_repr.find("(") + 1:-1]
        return (
            f"{self.__class__.__name__}("
            f"base_repr={_base_repr!r}, "
            f"conditions={_conditions!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _conditions=(
            self.conditions
            if hasattr(self, "conditions")
            else None
        )
        _base_str = super().__str__()
        _base_str = _base_str[_base_str.find("(") + 1:-1]
        return (
            f"{self.__class__.__name__}("
            f"base_str={_base_str!s}, "
            f"conditions={_conditions!s}, "
            f")"
        )
