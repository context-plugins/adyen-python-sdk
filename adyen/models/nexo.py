"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.event_url import EventUrl
from adyen.models.key import Key
from adyen.models.notification import Notification
from adyen.models.notification_url import NotificationUrl


class Nexo(object):
    """Implementation of the 'Nexo' model.

    Attributes:
        display_urls (NotificationUrl): The model property of type NotificationUrl.
        encryption_key (Key): The model property of type Key.
        event_urls (EventUrl): The model property of type EventUrl.
        nexo_event_urls (List[str]): One or more URLs to send event messages to when
            using Terminal API.
        notification (Notification): The model property of type Notification.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "display_urls": "displayUrls",
        "encryption_key": "encryptionKey",
        "event_urls": "eventUrls",
        "nexo_event_urls": "nexoEventUrls",
        "notification": "notification",
    }

    _optionals = [
        "display_urls",
        "encryption_key",
        "event_urls",
        "nexo_event_urls",
        "notification",
    ]

    def __init__(
        self,
        display_urls=APIHelper.SKIP,
        encryption_key=APIHelper.SKIP,
        event_urls=APIHelper.SKIP,
        nexo_event_urls=APIHelper.SKIP,
        notification=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a Nexo instance."""
        # Initialize members of the class
        if display_urls is not APIHelper.SKIP:
            self.display_urls = display_urls
        if encryption_key is not APIHelper.SKIP:
            self.encryption_key = encryption_key
        if event_urls is not APIHelper.SKIP:
            self.event_urls = event_urls
        if nexo_event_urls is not APIHelper.SKIP:
            self.nexo_event_urls = nexo_event_urls
        if notification is not APIHelper.SKIP:
            self.notification = notification

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        display_urls =\
            NotificationUrl.from_dictionary(
                dictionary.get("displayUrls"))\
                if "displayUrls" in dictionary.keys()\
                else APIHelper.SKIP
        encryption_key =\
            Key.from_dictionary(
                dictionary.get("encryptionKey"))\
                if "encryptionKey" in dictionary.keys()\
                else APIHelper.SKIP
        event_urls =\
            EventUrl.from_dictionary(
                dictionary.get("eventUrls"))\
                if "eventUrls" in dictionary.keys()\
                else APIHelper.SKIP
        nexo_event_urls =\
            dictionary.get("nexoEventUrls")\
            if dictionary.get("nexoEventUrls")\
                else APIHelper.SKIP
        notification =\
            Notification.from_dictionary(
                dictionary.get("notification"))\
                if "notification" in dictionary.keys()\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(display_urls,
                   encryption_key,
                   event_urls,
                   nexo_event_urls,
                   notification,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _display_urls=(
            self.display_urls
            if hasattr(self, "display_urls")
            else None
        )
        _encryption_key=(
            self.encryption_key
            if hasattr(self, "encryption_key")
            else None
        )
        _event_urls=(
            self.event_urls
            if hasattr(self, "event_urls")
            else None
        )
        _nexo_event_urls=(
            self.nexo_event_urls
            if hasattr(self, "nexo_event_urls")
            else None
        )
        _notification=(
            self.notification
            if hasattr(self, "notification")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"display_urls={_display_urls!r}, "
            f"encryption_key={_encryption_key!r}, "
            f"event_urls={_event_urls!r}, "
            f"nexo_event_urls={_nexo_event_urls!r}, "
            f"notification={_notification!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _display_urls=(
            self.display_urls
            if hasattr(self, "display_urls")
            else None
        )
        _encryption_key=(
            self.encryption_key
            if hasattr(self, "encryption_key")
            else None
        )
        _event_urls=(
            self.event_urls
            if hasattr(self, "event_urls")
            else None
        )
        _nexo_event_urls=(
            self.nexo_event_urls
            if hasattr(self, "nexo_event_urls")
            else None
        )
        _notification=(
            self.notification
            if hasattr(self, "notification")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"display_urls={_display_urls!s}, "
            f"encryption_key={_encryption_key!s}, "
            f"event_urls={_event_urls!s}, "
            f"nexo_event_urls={_nexo_event_urls!s}, "
            f"notification={_notification!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
