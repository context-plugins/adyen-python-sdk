"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
import dateutil.parser

from adyen.api_helper import APIHelper


class Passenger(object):
    """Implementation of the 'Passenger' model.

    Attributes:
        date_of_birth (date): The passenger's date of birth. * Format `yyyy-MM-dd` *
            minLength: 10 * maxLength: 10 * **additionalData key:**
            `airline.passenger[N].date_of_birth`
        first_name (str): The passenger's first name. > This field is required if the
            airline data includes passenger details or leg details. * Encoding: ASCII
            * **additionalData key:** `airline.passenger[N].first_name`
        last_name (str): The passenger's last name. > This field is required if the
            airline data includes passenger details or leg details. * Encoding: ASCII
            * **additionalData key:** `airline.passenger[N].last_name`
        phone_number (str): The passenger's phone number, including country code.
            This is an alphanumeric field that can include the '+' and '-' signs. *
            Encoding: ASCII * minLength: 3 characters * maxLength: 30 characters *
            **additionalData key:** `airline.passenger[N].phone_number`
        traveller_type (str): The IATA passenger type code (PTC). * Encoding: ASCII *
            minLength: 3 characters * maxLength: 6 characters * **additionalData
            key:** `airline.passenger[N].traveller_type`

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "date_of_birth": "dateOfBirth",
        "first_name": "firstName",
        "last_name": "lastName",
        "phone_number": "phoneNumber",
        "traveller_type": "travellerType",
    }

    _optionals = [
        "date_of_birth",
        "first_name",
        "last_name",
        "phone_number",
        "traveller_type",
    ]

    def __init__(
        self,
        date_of_birth=APIHelper.SKIP,
        first_name=APIHelper.SKIP,
        last_name=APIHelper.SKIP,
        phone_number=APIHelper.SKIP,
        traveller_type=APIHelper.SKIP):
        """Initialize a Passenger instance."""
        # Initialize members of the class
        if date_of_birth is not APIHelper.SKIP:
            self.date_of_birth = date_of_birth
        if first_name is not APIHelper.SKIP:
            self.first_name = first_name
        if last_name is not APIHelper.SKIP:
            self.last_name = last_name
        if phone_number is not APIHelper.SKIP:
            self.phone_number = phone_number
        if traveller_type is not APIHelper.SKIP:
            self.traveller_type = traveller_type

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        date_of_birth = dateutil.parser.parse(
            dictionary.get("dateOfBirth")).date()\
            if dictionary.get("dateOfBirth") else APIHelper.SKIP
        first_name =\
            dictionary.get("firstName")\
            if dictionary.get("firstName")\
                else APIHelper.SKIP
        last_name =\
            dictionary.get("lastName")\
            if dictionary.get("lastName")\
                else APIHelper.SKIP
        phone_number =\
            dictionary.get("phoneNumber")\
            if dictionary.get("phoneNumber")\
                else APIHelper.SKIP
        traveller_type =\
            dictionary.get("travellerType")\
            if dictionary.get("travellerType")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(date_of_birth,
                   first_name,
                   last_name,
                   phone_number,
                   traveller_type)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return True

        if not isinstance(dictionary, dict):
            return False

        return True

    def __repr__(self):
        """Return a unambiguous string representation."""
        _date_of_birth=(
            self.date_of_birth
            if hasattr(self, "date_of_birth")
            else None
        )
        _first_name=(
            self.first_name
            if hasattr(self, "first_name")
            else None
        )
        _last_name=(
            self.last_name
            if hasattr(self, "last_name")
            else None
        )
        _phone_number=(
            self.phone_number
            if hasattr(self, "phone_number")
            else None
        )
        _traveller_type=(
            self.traveller_type
            if hasattr(self, "traveller_type")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"date_of_birth={_date_of_birth!r}, "
            f"first_name={_first_name!r}, "
            f"last_name={_last_name!r}, "
            f"phone_number={_phone_number!r}, "
            f"traveller_type={_traveller_type!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _date_of_birth=(
            self.date_of_birth
            if hasattr(self, "date_of_birth")
            else None
        )
        _first_name=(
            self.first_name
            if hasattr(self, "first_name")
            else None
        )
        _last_name=(
            self.last_name
            if hasattr(self, "last_name")
            else None
        )
        _phone_number=(
            self.phone_number
            if hasattr(self, "phone_number")
            else None
        )
        _traveller_type=(
            self.traveller_type
            if hasattr(self, "traveller_type")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"date_of_birth={_date_of_birth!s}, "
            f"first_name={_first_name!s}, "
            f"last_name={_last_name!s}, "
            f"phone_number={_phone_number!s}, "
            f"traveller_type={_traveller_type!s}, "
            f")"
        )
