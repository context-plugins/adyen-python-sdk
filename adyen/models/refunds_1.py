"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.referenced_1 import Referenced1
from adyen.models.unreferenced_2 import Unreferenced2


class Refunds1(object):
    """Implementation of the 'Refunds1' model.

    Settings for refunds.

    Attributes:
        referenced (Referenced1): Settings for referenced refunds.
        unreferenced (Unreferenced2): Settings for unreferenced refunds.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "referenced": "referenced",
        "unreferenced": "unreferenced",
    }

    _optionals = [
        "referenced",
        "unreferenced",
    ]

    def __init__(
        self,
        referenced=APIHelper.SKIP,
        unreferenced=APIHelper.SKIP):
        """Initialize a Refunds1 instance."""
        # Initialize members of the class
        if referenced is not APIHelper.SKIP:
            self.referenced = referenced
        if unreferenced is not APIHelper.SKIP:
            self.unreferenced = unreferenced

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        referenced =\
            Referenced1.from_dictionary(
                dictionary.get("referenced"))\
                if "referenced" in dictionary.keys()\
                else APIHelper.SKIP
        unreferenced =\
            Unreferenced2.from_dictionary(
                dictionary.get("unreferenced"))\
                if "unreferenced" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(referenced,
                   unreferenced)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _referenced=(
            self.referenced
            if hasattr(self, "referenced")
            else None
        )
        _unreferenced=(
            self.unreferenced
            if hasattr(self, "unreferenced")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"referenced={_referenced!r}, "
            f"unreferenced={_unreferenced!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _referenced=(
            self.referenced
            if hasattr(self, "referenced")
            else None
        )
        _unreferenced=(
            self.unreferenced
            if hasattr(self, "unreferenced")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"referenced={_referenced!s}, "
            f"unreferenced={_unreferenced!s}, "
            f")"
        )
