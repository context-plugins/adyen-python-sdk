"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper
from adyen.models.transaction_id_type import (
    TransactionIDType,
)


class LoyaltyAcquirerData1(object):
    """Implementation of the 'LoyaltyAcquirerData1' model.

    Data related to the loyalty Acquirer during a loyalty transaction.
    If content not empty.

    Attributes:
        loyalty_acquirer_id (str): Identification of the loyalty Acquirer.
        approval_code (str): Code assigned to a transaction approval by the Acquirer.
            Could be an identifier of the approved transaction for the Acquirer. This
            data element is conditional for the Loyalty Acquirers. Used in the
            PaymentRequest request for a referral.
        loyalty_transaction_id (TransactionIDType): Identification of a transaction
            for the Sale System or the POI System.
        host_reconciliation_id (str): Identifier of a reconciliation period with a
            payment or loyalty host. Allows the assignment of a transaction to the
            Acquirer reconciliation (or batch).

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "loyalty_acquirer_id": "LoyaltyAcquirerID",
        "approval_code": "ApprovalCode",
        "loyalty_transaction_id": "LoyaltyTransactionID",
        "host_reconciliation_id": "HostReconciliationID",
    }

    _optionals = [
        "loyalty_acquirer_id",
        "approval_code",
        "loyalty_transaction_id",
        "host_reconciliation_id",
    ]

    def __init__(
        self,
        loyalty_acquirer_id=APIHelper.SKIP,
        approval_code=APIHelper.SKIP,
        loyalty_transaction_id=APIHelper.SKIP,
        host_reconciliation_id=APIHelper.SKIP):
        """Initialize a LoyaltyAcquirerData1 instance."""
        # Initialize members of the class
        if loyalty_acquirer_id is not APIHelper.SKIP:
            self.loyalty_acquirer_id = loyalty_acquirer_id
        if approval_code is not APIHelper.SKIP:
            self.approval_code = approval_code
        if loyalty_transaction_id is not APIHelper.SKIP:
            self.loyalty_transaction_id = loyalty_transaction_id
        if host_reconciliation_id is not APIHelper.SKIP:
            self.host_reconciliation_id = host_reconciliation_id

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        loyalty_acquirer_id =\
            dictionary.get("LoyaltyAcquirerID")\
            if dictionary.get("LoyaltyAcquirerID")\
                else APIHelper.SKIP
        approval_code =\
            dictionary.get("ApprovalCode")\
            if dictionary.get("ApprovalCode")\
                else APIHelper.SKIP
        loyalty_transaction_id =\
            TransactionIDType.from_dictionary(
                dictionary.get("LoyaltyTransactionID"))\
                if "LoyaltyTransactionID" in dictionary.keys()\
                else APIHelper.SKIP
        host_reconciliation_id =\
            dictionary.get("HostReconciliationID")\
            if dictionary.get("HostReconciliationID")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(loyalty_acquirer_id,
                   approval_code,
                   loyalty_transaction_id,
                   host_reconciliation_id)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _loyalty_acquirer_id=(
            self.loyalty_acquirer_id
            if hasattr(self, "loyalty_acquirer_id")
            else None
        )
        _approval_code=(
            self.approval_code
            if hasattr(self, "approval_code")
            else None
        )
        _loyalty_transaction_id=(
            self.loyalty_transaction_id
            if hasattr(self, "loyalty_transaction_id")
            else None
        )
        _host_reconciliation_id=(
            self.host_reconciliation_id
            if hasattr(self, "host_reconciliation_id")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"loyalty_acquirer_id={_loyalty_acquirer_id!r}, "
            f"approval_code={_approval_code!r}, "
            f"loyalty_transaction_id={_loyalty_transaction_id!r}, "
            f"host_reconciliation_id={_host_reconciliation_id!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _loyalty_acquirer_id=(
            self.loyalty_acquirer_id
            if hasattr(self, "loyalty_acquirer_id")
            else None
        )
        _approval_code=(
            self.approval_code
            if hasattr(self, "approval_code")
            else None
        )
        _loyalty_transaction_id=(
            self.loyalty_transaction_id
            if hasattr(self, "loyalty_transaction_id")
            else None
        )
        _host_reconciliation_id=(
            self.host_reconciliation_id
            if hasattr(self, "host_reconciliation_id")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"loyalty_acquirer_id={_loyalty_acquirer_id!s}, "
            f"approval_code={_approval_code!s}, "
            f"loyalty_transaction_id={_loyalty_transaction_id!s}, "
            f"host_reconciliation_id={_host_reconciliation_id!s}, "
            f")"
        )
