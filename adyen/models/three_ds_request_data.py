"""adyen.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from adyen.api_helper import APIHelper


class ThreeDSRequestData(object):
    """Implementation of the 'ThreeDSRequestData' model.

    Attributes:
        challenge_window_size (ChallengeWindowSizeEnum): Dimensions of the 3DS2
            challenge window to be displayed to the cardholder.  Possible values:  *
            **01** - size of 250x400  * **02** - size of 390x400 * **03** - size of
            500x600 * **04** - size of 600x400 * **05** - Fullscreen
        data_only (DataOnlyEnum): Required to trigger the [data-only
            flow](https://docs.adyen.com/online-payments/3d-secure/data-only/). When
            set to **true**, forces the 3D Secure 2 data-only flow for all
            transactions where it is possible.
        native_three_ds (NativeThreeDSEnum): Indicates if [native 3D Secure
            authentication](https://docs.adyen.com/online-payments/3d-secure/native-3d
            s2) should be triggered when available. Adyen can still select to
            fallback to the redirect flow to optimize authorization rates and improve
            the shopper's experience.  Possible values: * **preferred**: Use native
            3D Secure authentication when available. * **disabled**: Use the redirect
            3D Secure authentication flow.
        three_ds_version (ThreeDSVersionEnum): The version of 3D Secure to use.
            Possible values:  * **2.1.0** * **2.2.0**

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "challenge_window_size": "challengeWindowSize",
        "data_only": "dataOnly",
        "native_three_ds": "nativeThreeDS",
        "three_ds_version": "threeDSVersion",
    }

    _optionals = [
        "challenge_window_size",
        "data_only",
        "native_three_ds",
        "three_ds_version",
    ]

    def __init__(
        self,
        challenge_window_size=APIHelper.SKIP,
        data_only=APIHelper.SKIP,
        native_three_ds=APIHelper.SKIP,
        three_ds_version=APIHelper.SKIP):
        """Initialize a ThreeDSRequestData instance."""
        # Initialize members of the class
        if challenge_window_size is not APIHelper.SKIP:
            self.challenge_window_size = challenge_window_size
        if data_only is not APIHelper.SKIP:
            self.data_only = data_only
        if native_three_ds is not APIHelper.SKIP:
            self.native_three_ds = native_three_ds
        if three_ds_version is not APIHelper.SKIP:
            self.three_ds_version = three_ds_version

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        challenge_window_size =\
            dictionary.get("challengeWindowSize")\
            if dictionary.get("challengeWindowSize")\
                else APIHelper.SKIP
        data_only =\
            dictionary.get("dataOnly")\
            if dictionary.get("dataOnly")\
                else APIHelper.SKIP
        native_three_ds =\
            dictionary.get("nativeThreeDS")\
            if dictionary.get("nativeThreeDS")\
                else APIHelper.SKIP
        three_ds_version =\
            dictionary.get("threeDSVersion")\
            if dictionary.get("threeDSVersion")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(challenge_window_size,
                   data_only,
                   native_three_ds,
                   three_ds_version)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _challenge_window_size=(
            self.challenge_window_size
            if hasattr(self, "challenge_window_size")
            else None
        )
        _data_only=(
            self.data_only
            if hasattr(self, "data_only")
            else None
        )
        _native_three_ds=(
            self.native_three_ds
            if hasattr(self, "native_three_ds")
            else None
        )
        _three_ds_version=(
            self.three_ds_version
            if hasattr(self, "three_ds_version")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"challenge_window_size={_challenge_window_size!r}, "
            f"data_only={_data_only!r}, "
            f"native_three_ds={_native_three_ds!r}, "
            f"three_ds_version={_three_ds_version!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _challenge_window_size=(
            self.challenge_window_size
            if hasattr(self, "challenge_window_size")
            else None
        )
        _data_only=(
            self.data_only
            if hasattr(self, "data_only")
            else None
        )
        _native_three_ds=(
            self.native_three_ds
            if hasattr(self, "native_three_ds")
            else None
        )
        _three_ds_version=(
            self.three_ds_version
            if hasattr(self, "three_ds_version")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"challenge_window_size={_challenge_window_size!s}, "
            f"data_only={_data_only!s}, "
            f"native_three_ds={_native_three_ds!s}, "
            f"three_ds_version={_three_ds_version!s}, "
            f")"
        )
